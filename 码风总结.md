# 码风总结

> 基于 2026-09-13 对仓库 1902 个 `.cpp` 的抽样精读（约 25 份代表文件）+ 全库关键字统计归纳。

## 一、核心骨架（所有时期、所有场景通用）

### 头部与入口
- 永远 `#include <bits/stdc++.h>` + `using namespace std;`，从不手写 include 列表（写不出 `bits/stdc++.h` 的评测环境才启用模板中的 `BITS_NOT_ALLOWED` 全套头文件方案）。
- 入口签名从入门期起就用 `signed main()`（模板期定型为 `signed main(int argc, char *argv[])`），这是为 `#define int long long` 留的后门。工具脚本、Hello World 也不例外。
- I/O 三件套开头：`ios::sync_with_stdio(false); cin.tie(nullptr);`（或模板封装的 `IOSS_Init()`）。
- 输出换行用 `'\n'`，几乎不用 `endl`（271 处 vs `'\n'` 大量）；UVA 等多组输入用 `while (cin >> a >> b)`。

### 布局与括号
- 4 空格缩进，K&R 括号（`{` 跟在行尾），全库仅 8 个文件出现 Allman 裸大括号行。
- 行宽不设限：一行可以塞很长的表达式（含大量括号嵌套的公式直接一行写完）。
- 与根目录 `.clang-format` 一致：LLVM 基础、`IndentWidth: 4`、`ColumnLimit: 0`。

### 变量与状态
- 状态全放全局：数组开全局静态（不用 `vector` 建图），大小用 `const int N = 2e5 + 5` 式科学计数法上界。
- 变量名极短：`n, m, k, a[], b[], x, y, l, r, mid, ans, cnt, res`；同类型声明合并一行：`int n, m, d[N], h[N], to[N], ne[N], ec;`，极限时连读入的 `long long x, y; char c;` 都并排声明。
- 循环变量 `i/j/k`，2 的幂/状态用 `s`，临时交换用 `swap`。
- 溢出防护三选一：`#define int long long`（122 文件）、`typedef long long ll`（95 文件）、直接 `long long`（近期主流）；乘法前先 `% mod`，负数补 `+ m`。

### 惯用句法
- **逗号运算符压行**（标志性特征）：`cin >> n, ans1 = ans2 = 0;`、`cout << la << " " << i << "\n", la = i + 1, c++, s = 0;`
- **位运算替代算术/逻辑**：`mid = (l + r) >> 1`、`(条件) << 1) - 1`、`1LL << x`、`__builtin_popcount / __builtin_ctz`、`lg[i] = lg[(i+1)/2] + 1`。
- 自增以后置 `i++` 为主（3662 vs 1671），最新精简期文件转向前置 `++i`。
- 1-indexed 循环为主：`for (int i = 1; i <= n; i++)`（1303 处 vs `i < n` 472 处）。
- 单语句 `if/for` 视心情省大括号（`for (...) parent[i] = i;`），模板期则普遍补全大括号。
- 函数一律 `inline`，void 函数末尾显式 `return;`。
- 多测解法拆成 `solve()`，主函数循环调用。

### 固定算法手笔
- 图：链式前向星 `to[++ec] = y, ne[ec] = h[x], h[x] = ec` 的 `add()`；树/DP 场景用 `vector` 邻接表 `g[u].push_back(v)`。
- 并查集：`parent[]` + 路径压缩一行 `return parent[x] = find(parent[x]);`。
- 二分：`while (l < r)` + `mid = (l+r)>>1` 收敛式，或直接在答案上二分。
- 树剖/倍增：`up[j][i] = up[j-1][up[j-1][i]]`，`dfn` 命名 DFS 序。
- 离散化：`sort + unique + lower_bound` 三连，`m = unique(...) - w - 1`。
- 哈希容器防卡：`reserve` + `max_load_factor(0.7)`。
- 计时用 `clock()` 封装，调试输出走 `cerr`。
- I/O 应变：洛谷/比赛用 cin/cout；UVA、卡常几何题直接切 `scanf/printf`（同文件可混用）；少数题用快读宏 `ugetchar`。

### 命名与封装惯例
- 函数全小写无前缀，按出现频率：`solve`（1413 处 `inline void solve(`）、`dfs`（约 100）、`add`（65）、`build`（36）、`check`、`query`、`update`、`find`、`bfs`、`merge`。
- struct 命名分两档：**小 ADT 用一两个字母或小写单词**——`P`（点/对，18）、`E`、`Q`、`S`、`node`（58）、`Edge`；**封装型数据结构用全称大驼峰**——`BIT`（7）、`SegTree`（6）、`DSU`（6）、`State`、`Event`。结构体变量与数组可在声明处顺手定义：`struct P { long long x, y; } pt[N], stk[N];`。
- lambda 捕获 `[&]` 用得很重（738 个文件），模板还提供 `funct(name, ret, ...)` 宏把 lambda 伪装成函数定义。
- 简写宏两代并存：模板内 `#define All(x)`（575）与手写 `#define all(x)`（109）；另有 `ull`、`pii`、`#define double long double`（精度题）等零星简写。

## 二、三个时期的包装层

### 1. 入门期（B20xx/P1xxx/SXA 等）
裸骨架直出，无任何包装；省大括号更频繁；`#pragma G++ optimize(...)` 等也存在过。最早的长文件（高精度 BigInteger）是搬运的库代码，保留原作者 Google 风格，不算个人码风。

### 2. 模板期（主力，748/1902 文件）
个人大模板见 [template.cpp](template.cpp)（cph "Local: template" 指向）：
- CP Editor 自动文件头（Author/Time/Problem/Contest/URL/限制/Batch ID）+ `Copyright (C) 2026 TangYixiao`。
- `#define PRAGMA_TYPE` 三档编译优化开关 + `#pragma region` 折叠分区 + `// clang-format off` 保护。
- `namespace TANGYIXIAO` 内嵌 `IO`（FAST_IO / FILE_IO / INT128_IO）、`TIME`、`DEBUGS`（`std::format` 系宏 `fprint/ferr/funct`）、`BITS` 子命名空间，逐层 `using namespace` 打平。
- 多测由 `MULTIPLE_TEST` 宏控制：`int T = 1; #ifdef MULTIPLE_TEST cin >> T; #endif`。
- 题解代码写在文件末尾的 `namespace TANGYIXIAO { ... solve(int Task_Id) }` 里。
- 退出码 `return EXIT_SUCCESS;`。

### 3. 精简期（最新，如 T740260.cpp）
做减法回归裸骨架：无模板无命名空间，结构体 + 全局数组直出，`++i`、`long long` 显式写、`if (!len) return;` 式紧凑单行。可视为"模板期的肌肉记忆 + 入门期的形态"。

## 三、场景变体

- **对拍/造数据三件套**（gen / std / bf / 对拍驱动）：切换为**单行无空格极限压缩**风格，`sync_with_stdio(0)`，`system("./std < in > out")` 串联，`mt19937` 以命令行参数或时钟作种。与题解风格判若两人—— utility 专用。
- **JavaScript（Luogu2VjudgeExtension）**：完全标准化的现代 JS——2 空格缩进、IIFE + `"use strict"`、`const MAX_RETRIES` 大写蛇形常量、camelCase 函数、模板字符串、中文 JSDoc 注释。C++ 里从不写注释，JS 里注释齐全。
- **工具类 C++（LakeLopNur/Server）**：沿用同一骨架，但因非竞赛，变量名换成完整描述性 camelCase（`sourceFile`、`bufferSize`）。
- **C++ 竞赛代码正文零注释**；模板与 .md 文档里注释反而丰富。

## 四、文档与仓库习惯

- `.md` 两大类：题面存档（洛谷原题中文 + 英文翻译并列，保留样例/数据范围）与个人题解（第一人称"考虑……所以……"递进叙述，LaTeX 公式，末尾附完整 AC 代码）。
- 提交信息：日期式 `YYYY-MM-DD-HH-MM-SS`（可带描述后缀）。
- 文件命名：`题号 题名.cpp` / `#题号. 题名.cpp`（本地/学校 OJ 加 `#` 前缀）；同一题的对拍件用 `题号 …暴力.cpp / …数据生成.cpp / …对拍.cpp` 后缀区分。

## 五、非 C++ 资产的码风

### JavaScript（Luogu2VjudgeExtension）
与 C++ 判若两人——完全工业规范：2 空格缩进、IIFE + `"use strict"`、每个函数挂中文 JSDoc、`const MAX_RETRIES` 大写蛇形常量、Prettier 式换行与尾逗号。算法式思维还在：提取题号写了"方法1/方法2/方法3"三层 fallback 并逐层注释，`console.log/warn` 带统一前缀 `[洛谷跳转VJudge]` 做现场调试输出。

### LaTeX（Beamer 模板）
在浙大模板基础上二次开发，注释密度远超 C++：`% ============ 分区 ============` 与 `% --- xxx ---` 两级分隔注释、每个宏包一行并按用途分组打中文标签（字体/数学/图形）。改主题时保留原始选项注释备用（`% 浙大蓝，按需修改` → 启用博雅红），像写代码一样做"注释掉的配置分支"。构建走 Makefile，`xelatex` 连跑三遍 + bibtex。

### HTML/CSS（Server/）
教程跟练产物（w3schools 素材、英文注释 "Add more styling as per your requirement"）：HTML 2 空格小写标签、CSS 4 空格选择器风格，不代表个人风格主张。

### Markdown 语料构成
1127 篇 `.md` 中 956 篇为洛谷题面存档（中文原题 + 英文机翻并列，保留样例与数据范围），约 107 篇涉及题解/分析。题解文风：第一人称"考虑……所以……"递进叙述、关键结论加粗、`$$` 独立公式、末尾附完整 AC 代码——和代码一样"短平快"，不写客套话。

## 六、统计佐证

| 指标 | 数值 |
|---|---|
| 带个人模板（TANGYIXIAO）的文件 | 748 / 1902 |
| 带 CP Editor 头的文件 | 279 |
| `signed main` | 1076 |
| `freopen`（竞赛文件 IO） | 722 |
| `#define int long long` | 122 |
| `typedef long long ll` / `using ll = long long` | 95 / 21 |
| lambda `[&]` | 738 |
| `All(x)` / `all(x)` 宏 | 575 / 109 |
| 循环 `i <= n` vs `i < n` | 1303 vs 472 |
| `i++` vs `++i` | 3662 vs 1671 |
| `endl` | 271 |
| Allman 裸大括号文件 | 8 |

（注：`__gnu_pbds`、`fprint` 等宏的计数由模板文本自带抬高，不代表实际启用率。）

## 七、一句话画像

短名全局、一码到底的竞赛流 OI 选手：骨架（bits/stdc++、signed main、关同步、全局静态数组、K&R 四空格、1-indexed、逗号压行、位运算压逻辑）从入门第一天沿用至今；外壳经历了"裸写 → 自建 namespace 工业模板 → 精简化"三代，但省键击的美学始终如一。
