# P13473 题解

从 $(x,y)$ 到 $(u,v)$，设两种步子分别走 $a,b$ 次，则

$a+2b=u-x,\quad2a+b=v-y$。

解出非负整数 $a,b$ 后，路径数为 $\binom{a+b}{a}$；无整数解就是 0。坐标可达 $10^8$，组合数模 10007 用 Lucas 定理计算。

把起点、石头、终点按行列和排序。设 $d_i$ 为第一次到达第 $i$ 个关键点且此前不经过石头的路径数。先算起点到该点的总路径，再减去所有较早石头贡献：

$d_i=C(S,i)-\sum_{j<i}d_jC(j,i)$。

$R\le10$，每组只需 $O(R^2\log_{10007}(H+W))$，预处理阶乘为 $O(10007)$。小数据部分分用二维 DP。
