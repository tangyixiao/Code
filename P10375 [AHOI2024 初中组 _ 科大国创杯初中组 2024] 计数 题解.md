先考虑一个确定的糖果序列什么时候能吃完。

看当前最左边还没被吃掉的糖果。它最终被吃掉的时候，由于左边已经没有糖果了，所以它一定是某次操作的左端点，并且和右边某个同色糖果一起被吃掉。也就是说，整个序列可以从左往右划成若干段，每一段的首尾颜色相同。

于是对于一个确定的序列，设 $ok_i$ 表示前 $i$ 个糖果能不能吃完，$ok_0=1$，那么有

$$
ok_i=1\iff \exists j<i,\ ok_{j-1}=1\text{ 且 }c_j=c_i.
$$

直接按这个东西计数。

假设现在已经确定了前 $i$ 个糖果，考虑集合

$$
S_i=\{c_j\mid 1\le j\le i,\ ok_{j-1}=1\}.
$$

也就是所有可以作为下一段左端点的颜色。

那么加入第 $i+1$ 个糖果时：

* 如果它的颜色在 $S_i$ 中，那么一定有 $ok_{i+1}=1$；
* 否则一定有 $ok_{i+1}=0$。

显然我们并不关心 $S_i$ 里面具体有哪些颜色，只关心它有多少种颜色。

设 `f[j][0/1]` 表示当前 $|S_i|=j$，并且 $ok_i$ 为 $0/1$ 的序列数量。

假设当前 $|S_i|=j$。

如果新糖果选了 $S_i$ 中已有的颜色，一共有 $j$ 种选法。此时新的前缀一定合法，而且集合大小不变，所以

$$
g_{j,1}\mathrel{+}=(f_{j,0}+f_{j,1})j.
$$

如果选了一个 $S_i$ 外的颜色，一共有 $m-j$ 种选法，此时新的前缀一定不合法。

如果原来 $ok_i=0$，那么第 $i+1$ 个位置也不能作为下一段的开头，所以集合不变：

$$
g_{j,0}\mathrel{+}=f_{j,0}(m-j).
$$

如果原来 $ok_i=1$，那么第 $i+1$ 个位置可以作为下一段的开头。又因为它的颜色原本不在 $S_i$ 中，所以集合大小加一：

$$
g_{j+1,0}\mathrel{+}=f_{j,1}(m-j).
$$

初始时还没有放任何糖果，$S_0$ 为空，并且空前缀可以吃完，所以

$$
f_{0,1}=1.
$$

做完 $n$ 次转移后，把所有 `f[j][1]` 加起来就是答案。

状态只有 $O(n)$ 个，滚动数组一下，总共 $O(n^2)$，对于 $n\le3000$ 很轻松。

```cpp
#include <bits/stdc++.h>
using namespace std;

namespace TANGYIXIAO {

const int N=3e3+5;
const long long MOD=1e9+7;

int n;
long long m,f[2][N][2];

inline void solve(int Task_Id) {
    cin>>n>>m;

    f[0][0][1]=1;

    for (int i=0;i<n;i++) {
        int x=i&1,y=x^1;
        memset(f[y],0,sizeof(f[y]));

        for (int j=0;j<=i;j++) {
            long long in=j;
            long long out=(j<=m?m-j:0);

            long long s=(f[x][j][0]+f[x][j][1])%MOD;

            f[y][j][1]=(f[y][j][1]+s*in)%MOD;
            f[y][j][0]=(f[y][j][0]+f[x][j][0]*out)%MOD;
            f[y][j+1][0]=(f[y][j+1][0]+f[x][j][1]*out)%MOD;
        }
    }

    long long ans=0;

    for (int j=0;j<=n;j++) {
        ans=(ans+f[n&1][j][1])%MOD;
    }

    cout<<ans<<'\n';

    return;
}

} // namespace TANGYIXIAO

signed main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    TANGYIXIAO::solve(1);

    return 0;
}
```
