# CF1567D Expression Evaluation Error 题解

## 思路

设一个十进制数的各位数字为 $d_i$，那么 Alice 看到的数值为：

$$
\sum d_i \times 11^i
$$

因此，越高位的十进制数字，对答案的贡献越大。

注意到：

$$
10 \times 11^k < 11^{k+1}
$$

也就是说，把 $10$ 个 $10^k$ 合并成一个 $10^{k+1}$，十进制总和不变，但 Alice 计算出的结果会变大。因此应该尽量选择较大的 $10^k$ 作为一个数，避免把高位拆成许多低位数字。

对于当前剩余总和 $s$ 和还需要构造的数字个数 $n$：

1. 找到不超过 $s$ 的最大 $10^k$，记为 $x$；
2. 如果选择 $x$ 后，剩余的 $s-x$ 不足以给后面的每个数字分配至少 $1$，就令 $x$ 除以 $10$；
3. 输出 $x$，并令 $s\leftarrow s-x$；
4. 重复直到只剩最后一个数字，最后输出剩余的 $s$。

例如 $s=111,n=4$：

```text
先取 100，剩余 11
再取 1，剩余 10
再取 1，剩余 9
最后取 9
```

得到 `100 1 1 9`，其十进制和为 $111$，并且达到最大值。

## 正确性证明

设当前还需要构造 $m$ 个正整数，剩余总和为 $s$。

首先，若一个方案中存在可以向更高位合并的十进制部分，那么将 $10$ 个 $10^k$ 合并为一个 $10^{k+1}$ 后，十进制总和不变，而 Alice 的结果增加：

$$
11^{k+1}-10\times11^k=11^k>0
$$

所以最优方案一定尽可能保留更高位的部分。

贪心选择不超过当前 $s$ 的最大 $10^k$，并且保证剩余部分至少为 $m-1$。如果存在更大的可行选择，那么使用它可以把更多的数值放在更高的十进制位上，且不会影响剩余数字的正性，结果不会更差。因此贪心选择是最优的。

取出当前最优的 $x$ 后，剩余问题仍然是“用 $m-1$ 个正整数表示 $s-x$”，与原问题形式完全相同。根据归纳法，每一步的选择都最优，最后得到的整个构造也是最优的。

## 复杂度分析

每组数据中，$x$ 最多下降 $O(\log s)$ 次，外层循环执行 $n-1$ 次。

时间复杂度为 $O(n+\log s)$，空间复杂度为 $O(1)$。

## 代码实现

```cpp
#include <bits/stdc++.h>
using namespace std;

signed main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int T;
    cin >> T;
    while (T--) {
        int s, n;
        cin >> s >> n;
        int x = 1;
        while (x <= s / 10) {
            x *= 10;
        }
        for (int i = 1; i < n; i++) {
            while (s - x < n - i) {
                x /= 10;
            }
            cout << x << ' ';
            s -= x;
        }
        cout << s << '\n';
    }
    return 0;
}
```
