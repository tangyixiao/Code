# P4213 【模板】杜教筛 题解

预处理一段欧拉函数与莫比乌斯函数前缀。利用 $\varphi*1=id$ 和 $\mu*1=\varepsilon$，对超过预处理范围的 $n$ 递归分块：

$$S_\varphi(n)=\frac{n(n+1)}2-\sum_{l=2}^n(r-l+1)S_\varphi(\lfloor n/l\rfloor),$$

$$S_\mu(n)=1-\sum_{l=2}^n(r-l+1)S_\mu(\lfloor n/l\rfloor).$$

用哈希表记忆化相同商值。
