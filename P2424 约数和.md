# P2424 约数和

## 题目背景

Smart 最近沉迷于对约数的研究中。

## 题目描述

对于一个数 $X$，函数 $f(X)$ 表示 $X$ 所有约数的和。例如：$f(6)=1+2+3+6=12$。对于一个 $X$，Smart 可以很快的算出 $f(X)$。现在的问题是，给定两个正整数 $X,Y(X<Y)$，Smart 希望尽快地算出 $f(X)+f(X+1)+……+f(Y)$的值，你能帮助 Smart 算出这个值吗？

## 输入格式

输入文件仅一行，两个正整数 $X$ 和 $Y(X<Y)$，表示需要计算 $f(X)+f(X+1)+\dots +f(Y)$。

## 输出格式

输出只有一行，为 $f(X)+f(X+1)+\dots+f(Y)$ 的值。

## 输入输出样例 #1

### 输入 #1

```
2 4
```

### 输出 #1

```
14
```

## 输入输出样例 #2

### 输入 #2

```
123 321
```

### 输出 #2

```
72543
```

## 说明/提示

对于 $20\%$ 的数据有 $1\leq X<Y\leq 10^5$。

对于 $60\%$ 的数据有 $1\leq X<Y\leq 1\times 10^7$。

对于 $100\%$ 的数据有 $1\leq X<Y\leq 2\times 10^9$。

---

# P2424 Sum of Divisors

## 题目背景

Smart has recently become obsessed with the study of divisors.

## 题目描述

For a number $X$, the function $f(X)$ denotes the sum of all divisors of $X$. For example, $f(6)=1+2+3+6=12$. For a given $X$, Smart can quickly compute $f(X)$. Now, given two positive integers $X$ and $Y$ ($X<Y$), Smart wants to quickly compute the value of $f(X)+f(X+1)+\dots+f(Y)$. Can you help Smart compute this value?

## 输入格式

The input contains only one line with two positive integers $X$ and $Y$ ($X<Y$), indicating that you need to compute $f(X)+f(X+1)+\dots+f(Y)$.

## 输出格式

Output a single line containing the value of $f(X)+f(X+1)+\dots+f(Y)$.

## 输入输出样例 #1

### 输入 #1

```
2 4
```

### 输出 #1

```
14
```

## 输入输出样例 #2

### 输入 #2

```
123 321
```

### 输出 #2

```
72543
```

## 说明/提示

For $20\%$ of the testdata, $1 \leq X < Y \leq 10^5$.
For $60\%$ of the testdata, $1 \leq X < Y \leq 1\times 10^7$.
For $100\%$ of the testdata, $1 \leq X < Y \leq 2\times 10^9$.

Translated by ChatGPT 5