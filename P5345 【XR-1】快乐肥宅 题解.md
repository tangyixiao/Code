# P5345 【XR-1】快乐肥宅 题解

每天的体重等价于 $k_i^x\bmod g_i$，其中余数零按题意表示为 $g_i$。

对每个方程沿幂序列先剥离长度不超过 $O(\log g_i)$ 的尾部。若目标落在尾部，解唯一；否则约去稳定的 gcd 后用 BSGS 求循环中的最小解和乘法阶，得到 $x\equiv a_i\pmod {q_i}$ 且 $x\ge a_i$。唯一解直接逐式验证，其余用扩展 CRT 合并，再满足所有下界并检查 $10^9$ 限制。
