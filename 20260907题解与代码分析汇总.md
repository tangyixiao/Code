# P12001 在小小的奶龙山里面挖呀挖呀挖 题解

注意到一条路径需要搞好关系的公司数，其实就是路径上所有点权中出现过的**不同质因子个数**。

比如路径上有 $12,15,7$，那么涉及的质数就是 $2,3,5,7$，答案为 $4$。

所以问题变成：求树上一条路径上出现了多少种不同质因子。

考虑树上莫队。

先把每个 $a_i$ 分解质因数。这里有个比较舒服的性质：

$$
2\times3\times5\times7\times11\times13=30030\le 10^5,
$$

而

$$
2\times3\times5\times7\times11\times13\times17>10^5.
$$

所以每个 $a_i$ 至多只有 $6$ 个不同质因子，直接开 `f[N][7]` 存就行，不需要 `set` 之类的东西。

接下来考虑怎么把树上路径变成莫队区间。

DFS 的时候每个点记录两次，第一次进入时记 $tin_u$，退出时记 $tout_u$，得到长度为 $2n$ 的 Euler 序。

维护莫队区间时，每加入或删除一个位置，就把对应节点的状态翻转。一个点在区间中出现两次相当于没有出现，出现一次才算在当前路径里。

对于询问 $(u,v)$，先保证 $tin_u<tin_v$，令 $w=\operatorname{LCA}(u,v)$。

如果 $w=u$，说明 $u$ 是 $v$ 的祖先，直接取

$$
[tin_u,tin_v].
$$

否则取

$$
[tout_u,tin_v].
$$

此时区间对应的点集会少掉 LCA，所以处理这个询问时额外把 $w$ 翻转一次，记录完答案再翻转回来。

这样就变成普通莫队了。

维护答案也很直接。设 `c[p]` 表示当前点集中有多少个点包含质因子 $p$，`now` 表示当前存在多少种质因子。

加入一个节点时枚举它的所有质因子，如果原来 `c[p]==0` 就让 `now++`；删除时如果 `--c[p]==0` 就让 `now--`。

由于一个点最多只有 $6$ 个不同质因子，所以 `change` 的常数很小。题目还特意提醒常数，这里质数直接拿来当数组下标，不用离散化，也尽量别上 `map`、`set`。

莫队排序正常做奇偶块优化即可。块长我这里取 $2n/\sqrt q$。


```cpp
#include <bits/stdc++.h>
using namespace std;

namespace TANGYIXIAO {

const int N=5e4+5,A=1e5+5,L=17;

int n,q,a[N];
int mn[A],f[N][7],fc[N];
vector<int> g[N];

int dep[N],fa[N][L];
int tin[N],tout[N],e[N<<1],tim;

int vis[N],c[A],ans[N],now,B;

struct Node {
    int l,r,id,w,b;
} qu[N];

inline void init() {
    for (int i=2;i<A;i++) {
        if (!mn[i]) {
            mn[i]=i;
            if (1LL*i*i<A) {
                for (int j=i*i;j<A;j+=i) {
                    if (!mn[j]) {
                        mn[j]=i;
                    }
                }
            }
        }
    }
    return;
}

inline void dfs(int u,int p) {
    dep[u]=dep[p]+1;
    fa[u][0]=p;
    for (int j=1;j<L;j++) {
        fa[u][j]=fa[fa[u][j-1]][j-1];
    }

    tin[u]=++tim;
    e[tim]=u;

    for (int v:g[u]) {
        if (v!=p) {
            dfs(v,u);
        }
    }

    tout[u]=++tim;
    e[tim]=u;
    return;
}

inline int lca(int x,int y) {
    if (dep[x]<dep[y]) {
        swap(x,y);
    }

    int d=dep[x]-dep[y];
    for (int j=0;j<L;j++) {
        if ((d>>j)&1) {
            x=fa[x][j];
        }
    }

    if (x==y) {
        return x;
    }

    for (int j=L-1;j>=0;j--) {
        if (fa[x][j]!=fa[y][j]) {
            x=fa[x][j];
            y=fa[y][j];
        }
    }

    return fa[x][0];
}

inline void change(int x) {
    if (!vis[x]) {
        vis[x]=1;
        for (int j=0;j<fc[x];j++) {
            int p=f[x][j];
            if (!c[p]) {
                now++;
            }
            c[p]++;
        }
    } else {
        vis[x]=0;
        for (int j=0;j<fc[x];j++) {
            int p=f[x][j];
            c[p]--;
            if (!c[p]) {
                now--;
            }
        }
    }
    return;
}

inline bool cmp(const Node &x,const Node &y) {
    if (x.b!=y.b) {
        return x.b<y.b;
    }
    if (x.b&1) {
        return x.r>y.r;
    }
    return x.r<y.r;
}

inline void solve(int Task_Id) {
    cin>>n>>q;

    init();

    for (int i=1;i<=n;i++) {
        cin>>a[i];
        int x=a[i];
        while (x>1) {
            int p=mn[x];
            f[i][fc[i]++]=p;
            while (x%p==0) {
                x/=p;
            }
        }
    }

    for (int i=1;i<n;i++) {
        int u,v;
        cin>>u>>v;
        g[u].push_back(v);
        g[v].push_back(u);
    }

    dfs(1,0);

    B=max(1,(int)(tim/sqrt((double)q)));

    for (int i=1;i<=q;i++) {
        int u,v;
        cin>>u>>v;

        if (tin[u]>tin[v]) {
            swap(u,v);
        }

        int w=lca(u,v);

        if (w==u) {
            qu[i]={tin[u],tin[v],i,0,(tin[u]-1)/B};
        } else {
            qu[i]={tout[u],tin[v],i,w,(tout[u]-1)/B};
        }
    }

    sort(qu+1,qu+q+1,cmp);

    int l=1,r=0;

    for (int i=1;i<=q;i++) {
        while (l>qu[i].l) {
            change(e[--l]);
        }
        while (r<qu[i].r) {
            change(e[++r]);
        }
        while (l<qu[i].l) {
            change(e[l++]);
        }
        while (r>qu[i].r) {
            change(e[r--]);
        }

        if (qu[i].w) {
            change(qu[i].w);
        }

        ans[qu[i].id]=now;

        if (qu[i].w) {
            change(qu[i].w);
        }
    }

    for (int i=1;i<=q;i++) {
        cout<<ans[i]<<'\n';
    }

    return;
}

} // namespace TANGYIXIAO

signed main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    TANGYIXIAO::solve(1);

    return 0;
}
```

整体就是质因数分解 + 树上莫队，复杂度足够过 $n,q\le5\times10^4$。


---

# P10779 BZOJ4316 小 C 的独立集 题解

注意到题目保证每条边属于且仅属于一个简单环，所以这张图就是仙人掌，而且不会有桥。

这种图直接在原图上做独立集 DP 不太好处理环，考虑建圆方树。用 Tarjan 求点双，对于每个点双新建一个方点，把这个点双里的所有原点连向它。因为这里每个点双一定就是一个简单环，所以建出来的是一片森林。

设原点 $u$ 的 DP 为 $f_{u,0},f_{u,1}$，分别表示不选和选择 $u$ 时，圆方树子树内的最大独立集。

对于一个原点 $u$，如果它下面连着若干个环，那么这些环之间已经互不影响，直接加起来：

$$
f_{u,0}=\sum f_{v,0},
$$

$$
f_{u,1}=1+\sum f_{v,1},
$$

其中 $v$ 是 $u$ 的儿子方点。这里方点的 $f_{v,s}$ 表示：它的父亲原点状态为 $s$ 时，这个环以及下面部分的最优答案，且不重复计算父亲本身。

于是只剩下一个环怎么转移。

设当前方点对应的环按顺序是

$$
p,v_1,v_2,\ldots,v_k,
$$

其中 $p$ 是它在圆方树上的父亲。固定 $p$ 选或者不选之后，剩下的部分本质上就是一条链做独立集 DP，只需要注意最后的 $v_k$ 和 $p$ 之间还有一条边。

代码里用 `x0,x1` 表示当前走到的最后一个点不选或选择时的答案。转移就是普通链上独立集：

$$
y_0=\max(x_0,x_1)+f_{v,0},
$$

$$
y_1=x_0+f_{v,1}.
$$

如果 $p$ 被选，那么最后一个点一定不能选；否则两种状态取最大值即可。

还有一个实现上的问题：Tarjan 找到一个点双后，我们需要知道这个环上点的顺序。直接把这个点双里的边弹出来，因为它一定是简单环，所以每个点在当前点双内度数都是 $2$，临时记录每个点的两个邻点，随便找一个点一路走就能还原整个环。

图不一定连通，所以最后对圆方树森林的每个连通块分别做一次 DP。

```cpp
#include <bits/stdc++.h>
using namespace std;

namespace TANGYIXIAO {

const int N=5e4+5,M=6e4+5,T=12e4+5,INF=1e9;

int n,m,eu[M],ev[M];
vector<pair<int,int>> g[N];
vector<int> tr[T],cyc[T];

int dfn[N],low[N],tim,st[M],top,tot;
int deg[N],n1[N],n2[N];
int f[T][2],vis[T];

inline void build(vector<int> &e) {
    int o=++tot;
    vector<int> v;
    for (int id:e) {
        int x=eu[id],y=ev[id];
        if (!deg[x]) {
            v.push_back(x);
        }
        if (!deg[y]) {
            v.push_back(y);
        }
        if (!deg[x]) {
            n1[x]=y;
        } else {
            n2[x]=y;
        }
        if (!deg[y]) {
            n1[y]=x;
        } else {
            n2[y]=x;
        }
        deg[x]++;
        deg[y]++;
    }
    int s=v[0],p=0,u=s;
    for (;;) {
        cyc[o].push_back(u);
        int x=(n1[u]!=p?n1[u]:n2[u]);
        p=u;
        u=x;
        if (u==s) {
            break;
        }
    }
    for (int x:v) {
        tr[o].push_back(x);
        tr[x].push_back(o);
        deg[x]=0;
    }
    return;
}

inline void tarjan(int u,int pe) {
    dfn[u]=low[u]=++tim;
    for (auto [v,id]:g[u]) {
        if (id==pe) {
            continue;
        }
        if (!dfn[v]) {
            st[++top]=id;
            tarjan(v,id);
            low[u]=min(low[u],low[v]);
            if (low[v]>=dfn[u]) {
                vector<int> e;
                for (;;) {
                    int x=st[top--];
                    e.push_back(x);
                    if (x==id) {
                        break;
                    }
                }
                build(e);
            }
        } else if (dfn[v]<dfn[u]) {
            st[++top]=id;
            low[u]=min(low[u],dfn[v]);
        }
    }
    return;
}

inline void dfs(int u,int p) {
    vis[u]=1;
    if (u<=n) {
        f[u][0]=0;
        f[u][1]=1;
        for (int v:tr[u]) {
            if (v==p) {
                continue;
            }
            dfs(v,u);
            f[u][0]+=f[v][0];
            f[u][1]+=f[v][1];
        }
        return;
    }
    for (int v:tr[u]) {
        if (v!=p) {
            dfs(v,u);
        }
    }
    int k=cyc[u].size(),pos=0;
    for (int i=0;i<k;i++) {
        if (cyc[u][i]==p) {
            pos=i;
            break;
        }
    }
    for (int s=0;s<=1;s++) {
        int x0=(s?-INF:0),x1=(s?0:-INF);
        for (int i=1;i<k;i++) {
            int v=cyc[u][(pos+i)%k];
            int y0=max(x0,x1)+f[v][0];
            int y1=x0+f[v][1];
            x0=y0;
            x1=y1;
        }
        f[u][s]=(s?x0:max(x0,x1));
    }
    return;
}

inline void solve(int Task_Id) {
    cin>>n>>m;
    tot=n;
    for (int i=1;i<=m;i++) {
        int u,v;
        cin>>u>>v;
        eu[i]=u;
        ev[i]=v;
        g[u].push_back({v,i});
        g[v].push_back({u,i});
    }
    for (int i=1;i<=n;i++) {
        if (!dfn[i]) {
            tarjan(i,0);
            top=0;
        }
    }
    int ans=0;
    for (int i=1;i<=n;i++) {
        if (!vis[i]) {
            dfs(i,0);
            ans+=max(f[i][0],f[i][1]);
        }
    }
    cout<<ans<<'\n';
    return;
}

} // namespace TANGYIXIAO

signed main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    TANGYIXIAO::solve(1);

    return 0;
}
```


