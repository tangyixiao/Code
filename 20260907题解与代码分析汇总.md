# P12001 在小小的奶龙山里面挖呀挖呀挖 题解

注意到一条路径需要搞好关系的公司数，其实就是路径上所有点权中出现过的**不同质因子个数**。

比如路径上有 $12,15,7$，那么涉及的质数就是 $2,3,5,7$，答案为 $4$。

所以问题变成：求树上一条路径上出现了多少种不同质因子。

考虑树上莫队。

先把每个 $a_i$ 分解质因数。这里有个比较舒服的性质：

$$
2\times3\times5\times7\times11\times13=30030\le 10^5,
$$

而

$$
2\times3\times5\times7\times11\times13\times17>10^5.
$$

所以每个 $a_i$ 至多只有 $6$ 个不同质因子，直接开 `f[N][7]` 存就行，不需要 `set` 之类的东西。

接下来考虑怎么把树上路径变成莫队区间。

DFS 的时候每个点记录两次，第一次进入时记 $tin_u$，退出时记 $tout_u$，得到长度为 $2n$ 的 Euler 序。

维护莫队区间时，每加入或删除一个位置，就把对应节点的状态翻转。一个点在区间中出现两次相当于没有出现，出现一次才算在当前路径里。

对于询问 $(u,v)$，先保证 $tin_u<tin_v$，令 $w=\operatorname{LCA}(u,v)$。

如果 $w=u$，说明 $u$ 是 $v$ 的祖先，直接取

$$
[tin_u,tin_v].
$$

否则取

$$
[tout_u,tin_v].
$$

此时区间对应的点集会少掉 LCA，所以处理这个询问时额外把 $w$ 翻转一次，记录完答案再翻转回来。

这样就变成普通莫队了。

维护答案也很直接。设 `c[p]` 表示当前点集中有多少个点包含质因子 $p$，`now` 表示当前存在多少种质因子。

加入一个节点时枚举它的所有质因子，如果原来 `c[p]==0` 就让 `now++`；删除时如果 `--c[p]==0` 就让 `now--`。

由于一个点最多只有 $6$ 个不同质因子，所以 `change` 的常数很小。题目还特意提醒常数，这里质数直接拿来当数组下标，不用离散化，也尽量别上 `map`、`set`。

莫队排序正常做奇偶块优化即可。块长我这里取 $2n/\sqrt q$。


```cpp
#include <bits/stdc++.h>
using namespace std;

namespace TANGYIXIAO {

const int N=5e4+5,A=1e5+5,L=17;

int n,q,a[N];
int mn[A],f[N][7],fc[N];
vector<int> g[N];

int dep[N],fa[N][L];
int tin[N],tout[N],e[N<<1],tim;

int vis[N],c[A],ans[N],now,B;

struct Node {
    int l,r,id,w,b;
} qu[N];

inline void init() {
    for (int i=2;i<A;i++) {
        if (!mn[i]) {
            mn[i]=i;
            if (1LL*i*i<A) {
                for (int j=i*i;j<A;j+=i) {
                    if (!mn[j]) {
                        mn[j]=i;
                    }
                }
            }
        }
    }
    return;
}

inline void dfs(int u,int p) {
    dep[u]=dep[p]+1;
    fa[u][0]=p;
    for (int j=1;j<L;j++) {
        fa[u][j]=fa[fa[u][j-1]][j-1];
    }

    tin[u]=++tim;
    e[tim]=u;

    for (int v:g[u]) {
        if (v!=p) {
            dfs(v,u);
        }
    }

    tout[u]=++tim;
    e[tim]=u;
    return;
}

inline int lca(int x,int y) {
    if (dep[x]<dep[y]) {
        swap(x,y);
    }

    int d=dep[x]-dep[y];
    for (int j=0;j<L;j++) {
        if ((d>>j)&1) {
            x=fa[x][j];
        }
    }

    if (x==y) {
        return x;
    }

    for (int j=L-1;j>=0;j--) {
        if (fa[x][j]!=fa[y][j]) {
            x=fa[x][j];
            y=fa[y][j];
        }
    }

    return fa[x][0];
}

inline void change(int x) {
    if (!vis[x]) {
        vis[x]=1;
        for (int j=0;j<fc[x];j++) {
            int p=f[x][j];
            if (!c[p]) {
                now++;
            }
            c[p]++;
        }
    } else {
        vis[x]=0;
        for (int j=0;j<fc[x];j++) {
            int p=f[x][j];
            c[p]--;
            if (!c[p]) {
                now--;
            }
        }
    }
    return;
}

inline bool cmp(const Node &x,const Node &y) {
    if (x.b!=y.b) {
        return x.b<y.b;
    }
    if (x.b&1) {
        return x.r>y.r;
    }
    return x.r<y.r;
}

inline void solve(int Task_Id) {
    cin>>n>>q;

    init();

    for (int i=1;i<=n;i++) {
        cin>>a[i];
        int x=a[i];
        while (x>1) {
            int p=mn[x];
            f[i][fc[i]++]=p;
            while (x%p==0) {
                x/=p;
            }
        }
    }

    for (int i=1;i<n;i++) {
        int u,v;
        cin>>u>>v;
        g[u].push_back(v);
        g[v].push_back(u);
    }

    dfs(1,0);

    B=max(1,(int)(tim/sqrt((double)q)));

    for (int i=1;i<=q;i++) {
        int u,v;
        cin>>u>>v;

        if (tin[u]>tin[v]) {
            swap(u,v);
        }

        int w=lca(u,v);

        if (w==u) {
            qu[i]={tin[u],tin[v],i,0,(tin[u]-1)/B};
        } else {
            qu[i]={tout[u],tin[v],i,w,(tout[u]-1)/B};
        }
    }

    sort(qu+1,qu+q+1,cmp);

    int l=1,r=0;

    for (int i=1;i<=q;i++) {
        while (l>qu[i].l) {
            change(e[--l]);
        }
        while (r<qu[i].r) {
            change(e[++r]);
        }
        while (l<qu[i].l) {
            change(e[l++]);
        }
        while (r>qu[i].r) {
            change(e[r--]);
        }

        if (qu[i].w) {
            change(qu[i].w);
        }

        ans[qu[i].id]=now;

        if (qu[i].w) {
            change(qu[i].w);
        }
    }

    for (int i=1;i<=q;i++) {
        cout<<ans[i]<<'\n';
    }

    return;
}

} // namespace TANGYIXIAO

signed main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    TANGYIXIAO::solve(1);

    return 0;
}
```

整体就是质因数分解 + 树上莫队，复杂度足够过 $n,q\le5\times10^4$。


---

# P10779 BZOJ4316 小 C 的独立集 题解

注意到题目保证每条边属于且仅属于一个简单环，所以这张图就是仙人掌，而且不会有桥。

这种图直接在原图上做独立集 DP 不太好处理环，考虑建圆方树。用 Tarjan 求点双，对于每个点双新建一个方点，把这个点双里的所有原点连向它。因为这里每个点双一定就是一个简单环，所以建出来的是一片森林。

设原点 $u$ 的 DP 为 $f_{u,0},f_{u,1}$，分别表示不选和选择 $u$ 时，圆方树子树内的最大独立集。

对于一个原点 $u$，如果它下面连着若干个环，那么这些环之间已经互不影响，直接加起来：

$$
f_{u,0}=\sum f_{v,0},
$$

$$
f_{u,1}=1+\sum f_{v,1},
$$

其中 $v$ 是 $u$ 的儿子方点。这里方点的 $f_{v,s}$ 表示：它的父亲原点状态为 $s$ 时，这个环以及下面部分的最优答案，且不重复计算父亲本身。

于是只剩下一个环怎么转移。

设当前方点对应的环按顺序是

$$
p,v_1,v_2,\ldots,v_k,
$$

其中 $p$ 是它在圆方树上的父亲。固定 $p$ 选或者不选之后，剩下的部分本质上就是一条链做独立集 DP，只需要注意最后的 $v_k$ 和 $p$ 之间还有一条边。

代码里用 `x0,x1` 表示当前走到的最后一个点不选或选择时的答案。转移就是普通链上独立集：

$$
y_0=\max(x_0,x_1)+f_{v,0},
$$

$$
y_1=x_0+f_{v,1}.
$$

如果 $p$ 被选，那么最后一个点一定不能选；否则两种状态取最大值即可。

还有一个实现上的问题：Tarjan 找到一个点双后，我们需要知道这个环上点的顺序。直接把这个点双里的边弹出来，因为它一定是简单环，所以每个点在当前点双内度数都是 $2$，临时记录每个点的两个邻点，随便找一个点一路走就能还原整个环。

图不一定连通，所以最后对圆方树森林的每个连通块分别做一次 DP。

```cpp
#include <bits/stdc++.h>
using namespace std;

namespace TANGYIXIAO {

const int N=5e4+5,M=6e4+5,T=12e4+5,INF=1e9;

int n,m,eu[M],ev[M];
vector<pair<int,int>> g[N];
vector<int> tr[T],cyc[T];

int dfn[N],low[N],tim,st[M],top,tot;
int deg[N],n1[N],n2[N];
int f[T][2],vis[T];

inline void build(vector<int> &e) {
    int o=++tot;
    vector<int> v;
    for (int id:e) {
        int x=eu[id],y=ev[id];
        if (!deg[x]) {
            v.push_back(x);
        }
        if (!deg[y]) {
            v.push_back(y);
        }
        if (!deg[x]) {
            n1[x]=y;
        } else {
            n2[x]=y;
        }
        if (!deg[y]) {
            n1[y]=x;
        } else {
            n2[y]=x;
        }
        deg[x]++;
        deg[y]++;
    }
    int s=v[0],p=0,u=s;
    for (;;) {
        cyc[o].push_back(u);
        int x=(n1[u]!=p?n1[u]:n2[u]);
        p=u;
        u=x;
        if (u==s) {
            break;
        }
    }
    for (int x:v) {
        tr[o].push_back(x);
        tr[x].push_back(o);
        deg[x]=0;
    }
    return;
}

inline void tarjan(int u,int pe) {
    dfn[u]=low[u]=++tim;
    for (auto [v,id]:g[u]) {
        if (id==pe) {
            continue;
        }
        if (!dfn[v]) {
            st[++top]=id;
            tarjan(v,id);
            low[u]=min(low[u],low[v]);
            if (low[v]>=dfn[u]) {
                vector<int> e;
                for (;;) {
                    int x=st[top--];
                    e.push_back(x);
                    if (x==id) {
                        break;
                    }
                }
                build(e);
            }
        } else if (dfn[v]<dfn[u]) {
            st[++top]=id;
            low[u]=min(low[u],dfn[v]);
        }
    }
    return;
}

inline void dfs(int u,int p) {
    vis[u]=1;
    if (u<=n) {
        f[u][0]=0;
        f[u][1]=1;
        for (int v:tr[u]) {
            if (v==p) {
                continue;
            }
            dfs(v,u);
            f[u][0]+=f[v][0];
            f[u][1]+=f[v][1];
        }
        return;
    }
    for (int v:tr[u]) {
        if (v!=p) {
            dfs(v,u);
        }
    }
    int k=cyc[u].size(),pos=0;
    for (int i=0;i<k;i++) {
        if (cyc[u][i]==p) {
            pos=i;
            break;
        }
    }
    for (int s=0;s<=1;s++) {
        int x0=(s?-INF:0),x1=(s?0:-INF);
        for (int i=1;i<k;i++) {
            int v=cyc[u][(pos+i)%k];
            int y0=max(x0,x1)+f[v][0];
            int y1=x0+f[v][1];
            x0=y0;
            x1=y1;
        }
        f[u][s]=(s?x0:max(x0,x1));
    }
    return;
}

inline void solve(int Task_Id) {
    cin>>n>>m;
    tot=n;
    for (int i=1;i<=m;i++) {
        int u,v;
        cin>>u>>v;
        eu[i]=u;
        ev[i]=v;
        g[u].push_back({v,i});
        g[v].push_back({u,i});
    }
    for (int i=1;i<=n;i++) {
        if (!dfn[i]) {
            tarjan(i,0);
            top=0;
        }
    }
    int ans=0;
    for (int i=1;i<=n;i++) {
        if (!vis[i]) {
            dfs(i,0);
            ans+=max(f[i][0],f[i][1]);
        }
    }
    cout<<ans<<'\n';
    return;
}

} // namespace TANGYIXIAO

signed main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    TANGYIXIAO::solve(1);

    return 0;
}
```


# P8819 [CSP-S 2022] 星战


## 思路

注意到题目要求每个点恰好只有一条可用出边。只要这个条件成立，从任意点开始沿唯一出边一直走，因为点数有限，最后一定会走进一个环，所以题目的第一个条件其实不用额外考虑。

记点 $u$ 当前的可用出度为 $d_u$，我们只需要判断

$$
d_u=1\qquad(1\le u\le n).
$$

考虑维护

$$
E=\sum_{u=1}^n d_u
$$

和

$$
P=\sum_{u=1}^n\binom{d_u}{2}.
$$

由于所有 $d_u$ 都是非负整数，有

$$
P=0\iff d_u\in\{0,1\}.
$$

所以

$$
E=n,\ P=0
$$

当且仅当所有 $d_u=1$。

这样就完全不用随机哈希了。

如果某个点的当前出度为 $x$，那么：

* $x\to x+1$ 时，$P$ 增加 $x$；
* $x\to x-1$ 时，$P$ 减少 $x-1$。

所以单边修改很好处理。

对于终点 $v$，记它在原图中的所有入边起点组成集合

$$
S_v=\{u\mid (u,v)\in E_0\}.
$$

设整列操作前

$$
T_v=\sum_{u\in S_v}d_u.
$$

如果 $S_v$ 内所有点的出度同时 $+1$，那么

$$
\Delta P=T_v.
$$

如果同时 $-1$，那么

$$
\Delta P=|S_v|-T_v.
$$

于是问题就变成了：支持单点修改 $d_u\pm1$，以及对一个静态集合 $S_v$ 整体 $\pm1$，同时快速求出 $T_v$。


这里有个坑。操作 $2,4$ 会直接修改一个终点的全部入边，但这些边之前可能已经被操作 $1,3$ 单独改过。

对每个终点 $v$ 维护一个基准状态 $b_v\in{0,1}$，以及例外集合 $X_v$，表示当前状态和 $b_v$ 不同的边。

初始显然有

$$
b_v=1,\qquad X_v=\varnothing.
$$

操作 $1,3$ 只改单条边。修改以后，如果这条边的状态等于 $b_v$，就从 $X_v$ 删除，否则加入 $X_v$。

操作 $2,4$ 时，先把 $X_v$ 全部恢复到基准状态并清空。如果当前 $b_v$ 和目标状态不同，再对整个 $S_v$ 做一次整体修改并更新 $b_v$。

所有被清掉的例外都一定来自之前的一次单边操作，所以这部分总共只会处理 $O(q)$ 条例外。代码里只给真正出现在 $1,3$ 操作中的边编号，再用 swap-delete 维护即可。


取阈值 $B$。

如果

$$
|S_v|>B,
$$

称 $v$ 为重终点，否则为轻终点。

设重终点数量为 $H$，显然

$$
H\le \frac mB.
$$

把对应集合编号为 $G_1,G_2,\ldots,G_H$。

对于每个重集合维护懒标记 $tag_h$，令

$$
d_u=val_u+\sum_{h:u\in G_h}tag_h.
$$

初始所有重集合对应的边都存在，所以 $tag_h=1$，轻边的贡献直接放进 $val_u$。

再维护

$$
hs_h=\sum_{u\in G_h}val_u.
$$

### 单点修改

修改点 $u$ 时，先通过

$$
d_u=val_u+\sum_{h:u\in G_h}tag_h
$$

求出修改前的真实出度，然后更新 $E,P$。

接着修改 $val_u$。

由于 $u$ 可能同时属于若干个重集合，所以对于所有满足 $u\in G_h$ 的 $h$，还要同步修改 $hs_h$。

### 重集合修改

预处理

$$
hh_{h,k}=|G_h\cap G_k|.
$$

那么

$$
\sum_{u\in G_h}d_u
=
hs_h+\sum_{k=1}^Hhh_{h,k}tag_k.
$$

所以重集合 $G_h$ 整体修改时，不需要枚举集合中的点，直接利用上式求出 $T_h$，更新 $E,P$，最后修改对应的 $tag_h$ 即可。

### 轻集合修改

轻集合大小至多为 $B$，所以它的 $val$ 部分直接枚举。

再记

$$
c_{v,h}=|S_v\cap G_h|.
$$

于是

$$
T_v
=
\sum_{u\in S_v}val_u
+
\sum_{h=1}^Hc_{v,h}tag_h.
$$

整体修改时直接枚举 $S_v$ 修改对应的 $val_u$，并根据 $c_{v,h}$ 同步修改各个 $hs_h$。

实际代码里只对真正出现过操作 $2,4$ 的轻终点建这部分信息。交比较少就用稀疏表示，交比较多就连续存储，这些都只是压常，不影响做法。

最后每次询问后判断

$$
E=n\ \text{且}\ P=0
$$

即可。

这套做法没有随机权值，也不存在哈希冲突。

代码这里就不给了。


---

# P10375 [AHOI2024 初中组 _ 科大国创杯初中组 2024] 计数 题解

先考虑一个确定的糖果序列什么时候能吃完。

看当前最左边还没被吃掉的糖果。它最终被吃掉的时候，由于左边已经没有糖果了，所以它一定是某次操作的左端点，并且和右边某个同色糖果一起被吃掉。也就是说，整个序列可以从左往右划成若干段，每一段的首尾颜色相同。

于是对于一个确定的序列，设 $ok_i$ 表示前 $i$ 个糖果能不能吃完，$ok_0=1$，那么有

$$
ok_i=1\iff \exists j<i,\ ok_{j-1}=1\text{ 且 }c_j=c_i.
$$

直接按这个东西计数。

假设现在已经确定了前 $i$ 个糖果，考虑集合

$$
S_i=\{c_j\mid 1\le j\le i,\ ok_{j-1}=1\}.
$$

也就是所有可以作为下一段左端点的颜色。

那么加入第 $i+1$ 个糖果时：

* 如果它的颜色在 $S_i$ 中，那么一定有 $ok_{i+1}=1$；
* 否则一定有 $ok_{i+1}=0$。

显然我们并不关心 $S_i$ 里面具体有哪些颜色，只关心它有多少种颜色。

设 `f[j][0/1]` 表示当前 $|S_i|=j$，并且 $ok_i$ 为 $0/1$ 的序列数量。

假设当前 $|S_i|=j$。

如果新糖果选了 $S_i$ 中已有的颜色，一共有 $j$ 种选法。此时新的前缀一定合法，而且集合大小不变，所以

$$
g_{j,1}\mathrel{+}=(f_{j,0}+f_{j,1})j.
$$

如果选了一个 $S_i$ 外的颜色，一共有 $m-j$ 种选法，此时新的前缀一定不合法。

如果原来 $ok_i=0$，那么第 $i+1$ 个位置也不能作为下一段的开头，所以集合不变：

$$
g_{j,0}\mathrel{+}=f_{j,0}(m-j).
$$

如果原来 $ok_i=1$，那么第 $i+1$ 个位置可以作为下一段的开头。又因为它的颜色原本不在 $S_i$ 中，所以集合大小加一：

$$
g_{j+1,0}\mathrel{+}=f_{j,1}(m-j).
$$

初始时还没有放任何糖果，$S_0$ 为空，并且空前缀可以吃完，所以

$$
f_{0,1}=1.
$$

做完 $n$ 次转移后，把所有 `f[j][1]` 加起来就是答案。

状态只有 $O(n)$ 个，滚动数组一下，总共 $O(n^2)$，对于 $n\le3000$ 很轻松。

```cpp
#include <bits/stdc++.h>
using namespace std;

namespace TANGYIXIAO {

const int N=3e3+5;
const long long MOD=1e9+7;

int n;
long long m,f[2][N][2];

inline void solve(int Task_Id) {
    cin>>n>>m;

    f[0][0][1]=1;

    for (int i=0;i<n;i++) {
        int x=i&1,y=x^1;
        memset(f[y],0,sizeof(f[y]));

        for (int j=0;j<=i;j++) {
            long long in=j;
            long long out=(j<=m?m-j:0);

            long long s=(f[x][j][0]+f[x][j][1])%MOD;

            f[y][j][1]=(f[y][j][1]+s*in)%MOD;
            f[y][j][0]=(f[y][j][0]+f[x][j][0]*out)%MOD;
            f[y][j+1][0]=(f[y][j+1][0]+f[x][j][1]*out)%MOD;
        }
    }

    long long ans=0;

    for (int j=0;j<=n;j++) {
        ans=(ans+f[n&1][j][1])%MOD;
    }

    cout<<ans<<'\n';

    return;
}

} // namespace TANGYIXIAO

signed main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    TANGYIXIAO::solve(1);

    return 0;
}
```


---


# P3332 [ZJOI2013] K 大数查询 题解

注意到修改虽然是在一段位置里插入同一个数，但是询问本质还是一个**区间第 $k$ 大**。

如果直接在线做，很容易想到权值线段树套区间线段树，不过这题可以直接整体二分，空间小很多。

## 思路

考虑当前正在处理的答案值域 $[L,R]$，令

$$
mid=L+\left\lfloor\frac{R-L+1}{2}\right\rfloor.
$$

我们把值域拆成：

* 左边 $[L,mid-1]$；
* 右边 $[mid,R]$。

因为求的是第 $k$ **大**，所以先考虑右边。

对于一条修改

```text
1 l r c
```

如果 $c\ge mid$，那么这个数属于右半边。我们在线段树上把 $[l,r]$ 区间加一。

这样，对于一条询问

```text
2 l r k
```

查询线段树中 $[l,r]$ 的区间和，设为 $x$。

这里的 $x$ 就表示：**在当前询问发生以前，$[l,r]$ 中一共有多少个值不小于 $mid$ 的数。**

于是：

* 如果 $x\ge k$，第 $k$ 大一定在右半边；
* 否则答案在左半边，并且右半边的 $x$ 个数已经全部跳过，所以令

$$
k\leftarrow k-x.
$$

然后把所有操作按照去左边还是右边稳定地分开，继续递归即可。

这里一定要**保持操作原来的相对顺序**。因为一条询问只能看见它之前的修改，整体二分虽然会重新排列操作，但是同一个子问题内部的时间顺序不能乱。

每一层处理完成以后，别忘了把这一层在线段树上的修改撤销。

## 线段树维护什么

一次修改 `1 l r c`，如果当前认为它属于右半值域，就相当于：

$$
[l,r]\gets [l,r]+1.
$$

询问则需要求某个区间的和。

所以普通的**区间加、区间求和线段树**就够了。

这里有一个比较阴的坑：

$$
nm=5\times10^4\times5\times10^4=2.5\times10^9,
$$

已经超过 `int`。

而且题面直接给了询问中的 $c<2^{63}$，因此线段树的 `sum`、`tag` 和询问的 $k$ 全部开 `long long`。

另外值域有负数，别写一些依赖负数右移的奇怪 `mid`，直接写：

```cpp
int mid=vl+(vr-vl+1)/2;
```

最省心。

复杂度为 $O(m\log n\log n)$，空间 $O(n+m)$。

## 代码

```cpp
#include <bits/stdc++.h>
using namespace std;

namespace TANGYIXIAO {
    const int N=5e4+5;

    struct Node {
        int op,l,r,id;
        long long c;
    } a[N],b[N];

    int n,m,cnt,ans[N];
    long long tr[N<<2],tag[N<<2];
    bool go[N];

    void push(int p,int l,int r) {
        if (!tag[p] || l==r) {
            return;
        }
        int mid=(l+r)>>1;
        tr[p<<1]+=tag[p]*(mid-l+1);
        tr[p<<1|1]+=tag[p]*(r-mid);
        tag[p<<1]+=tag[p];
        tag[p<<1|1]+=tag[p];
        tag[p]=0;
    }

    void add(int p,int l,int r,int ql,int qr,long long v) {
        if (ql<=l && r<=qr) {
            tr[p]+=v*(r-l+1);
            tag[p]+=v;
            return;
        }
        push(p,l,r);
        int mid=(l+r)>>1;
        if (ql<=mid) {
            add(p<<1,l,mid,ql,qr,v);
        }
        if (qr>mid) {
            add(p<<1|1,mid+1,r,ql,qr,v);
        }
        tr[p]=tr[p<<1]+tr[p<<1|1];
    }

    long long ask(int p,int l,int r,int ql,int qr) {
        if (ql<=l && r<=qr) {
            return tr[p];
        }
        push(p,l,r);
        int mid=(l+r)>>1;
        long long res=0;
        if (ql<=mid) {
            res+=ask(p<<1,l,mid,ql,qr);
        }
        if (qr>mid) {
            res+=ask(p<<1|1,mid+1,r,ql,qr);
        }
        return res;
    }

    void solve(int ql,int qr,int vl,int vr) {
        if (ql>qr) {
            return;
        }
        if (vl==vr) {
            for (int i=ql;i<=qr;i++) {
                if (a[i].op==2) {
                    ans[a[i].id]=vl;
                }
            }
            return;
        }

        int mid=vl+(vr-vl+1)/2;

        for (int i=ql;i<=qr;i++) {
            if (a[i].op==1) {
                if (a[i].c>=mid) {
                    add(1,1,n,a[i].l,a[i].r,1);
                    go[i]=1;
                } else {
                    go[i]=0;
                }
            } else {
                long long x=ask(1,1,n,a[i].l,a[i].r);
                if (x>=a[i].c) {
                    go[i]=1;
                } else {
                    a[i].c-=x;
                    go[i]=0;
                }
            }
        }

        for (int i=ql;i<=qr;i++) {
            if (a[i].op==1 && a[i].c>=mid) {
                add(1,1,n,a[i].l,a[i].r,-1);
            }
        }

        int p=ql;
        for (int i=ql;i<=qr;i++) {
            if (!go[i]) {
                b[p++]=a[i];
            }
        }
        int k=p-1;
        for (int i=ql;i<=qr;i++) {
            if (go[i]) {
                b[p++]=a[i];
            }
        }
        for (int i=ql;i<=qr;i++) {
            a[i]=b[i];
        }

        solve(ql,k,vl,mid-1);
        solve(k+1,qr,mid,vr);
    }
}

signed main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    using namespace TANGYIXIAO;

    cin>>n>>m;
    for (int i=1;i<=m;i++) {
        cin>>a[i].op>>a[i].l>>a[i].r>>a[i].c;
        if (a[i].op==2) {
            a[i].id=++cnt;
        }
    }

    solve(1,m,-n,n);

    for (int i=1;i<=cnt;i++) {
        cout<<ans[i]<<'\n';
    }

    return 0;
}
```
