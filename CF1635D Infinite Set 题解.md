# CF1635D Infinite Set题解

## 思路

把整数写成二进制，可以发现：

- $2y+1$ 相当于在 $y$ 的二进制末尾添加一个 `1`；
- $4y$ 相当于在 $y$ 的二进制末尾添加两个 `0`。

因此，一个初始数字能够生成的所有数字，就是在它的二进制末尾不断添加 `1` 或 `00` 得到的数字。

### 去除重复的初始数字

如果一个初始数字可以由另一个初始数字生成，那么它产生的集合已经被另一个初始数字完全包含，可以直接删除。

对于一个数字 $x$，它的逆操作是唯一的：

- 如果 $x$ 是奇数，可以令 $x=(x-1)/2$；
- 如果 $x$ 能被 $4$ 整除，可以令 $x=x/4$；
- 如果 $x \bmod 4=2$，则不能继续反推。

沿着逆操作不断寻找，如果中途遇到了另一个初始数字，就删除当前数字。例如 $20\to5$，如果 $5$ 在初始数组中，则 $20$ 是多余的。

逆操作的路径是唯一的，所以去重后不同初始数字生成的集合不会有交集。

### 计算一个初始数字的贡献

设初始数字的二进制长度为 $l$。严格小于 $2^p$ 等价于二进制长度不超过 $p$，所以最多还能添加 $p-l$ 位。

令 $f_i$ 表示长度恰好为 $i$ 的合法后缀数量。最后添加的部分只有两种情况：

- 最后添加 `1`，贡献 $f_{i-1}$；
- 最后添加 `00`，贡献 $f_{i-2}$。

于是有：

$$
f_0=1,\quad f_1=1,\quad f_i=f_{i-1}+f_{i-2}
$$

再令 $g_i=\sum_{j=0}^{i}f_j$，则长度为 $l$ 的初始数字贡献 $g_{p-l}$ 个数字。

预处理 $f$ 和 $g$ 后，枚举所有没有被删除的初始数字累加贡献即可。

## 正确性说明

每个合法数字的最后一步是唯一确定的：末尾为 `1` 时只能由 $2y+1$ 得到，末尾为 `00` 时只能由 $4y$ 得到，末尾为 `10` 时不能继续反推。因此，如果一个初始数字的逆向路径中出现了另一个初始数字，它生成的所有数字都已经被后者生成，删除它不会影响答案；反之，去重后不同初始数字的生成集合不会相交。

对于一个保留的初始数字，所有后缀都由长度为 $1$ 的 `1` 和长度为 $2$ 的 `00` 拼接而成。按最后一个拼接块分类，可以得到 $f_i=f_{i-1}+f_{i-2}$，所以 $g_{p-l}$ 恰好统计了所有长度不超过 $p$ 的后缀，也就是该初始数字对答案的全部贡献。

将所有互不相交的生成集合的贡献相加，得到的就是集合 $S$ 中严格小于 $2^p$ 的元素总数。

## 复杂度分析

每个数字的逆向路径长度最多为 $O(\log a_i)$，总复杂度为 $O(p+n\log A)$，其中 $A=10^9$；空间复杂度为 $O(p+n)$。

所有计数均对 $10^9+7$ 取模。
