# CF1627D Not Adding 题解

## 题意

给定一个由不同正整数组成的数组。每次可以选择数组中的两个数，将它们的最大公约数加入数组，但这个最大公约数不能已经存在。

求最多可以进行多少次操作。

## 思路

设一个数 `x` 最终可以被加入数组。

如果若干个原数组中的数的最大公约数为 `x`，那么可以按照下面的方式逐步得到 `x`：

1. 先取其中两个数求 gcd；
2. 再让得到的 gcd 与下一个数求 gcd；
3. 重复这个过程，直到得到 `x`。

中间某个 gcd 如果已经在数组中，就不需要重复加入，直接继续使用即可。

因此，`x` 可以被加入，当且仅当：

- `x` 原本不在数组中；
- 所有能被 `x` 整除的原数组元素的 gcd 恰好等于 `x`。

为什么只需要考虑 `x` 的倍数？因为如果一组数的 gcd 是 `x`，那么这组数中的每个数都必须是 `x` 的倍数。

## 算法

用 `vis[i]` 表示 `i` 是否在原数组中。

对于每个没有出现过的 `i`：

1. 枚举 `i, 2i, 3i, ...`；
2. 求所有存在于原数组中的倍数的 gcd；
3. 如果 gcd 等于 `i`，答案加一。

## 正确性证明

对于任意未出现的数 `x`：

- 若所有 `x` 的倍数的 gcd 为 `x`，则这些倍数中的一部分原数组元素的 gcd 为 `x`。依次进行两数 gcd 操作即可得到 `x`，所以 `x` 可以被加入。
- 若所有 `x` 的倍数的 gcd 不为 `x`，那么任意原数组元素子集的 gcd 也不可能为 `x`。由于后续加入的数本身也只能是原数组某个子集的 gcd，因此 `x` 不可能被加入。

所以，算法恰好统计出了所有能够被加入的数，答案正确。

## 复杂度分析

设 `A=10^6`。枚举所有倍数的总复杂度为调和级数：

$$
O\left(A\sum_{i=1}^{A}\frac{1}{i}\right)=O(A\log A)
$$

空间复杂度为 `O(A)`。

## 参考代码

见 [CF1627D Not Adding.cpp](<CF1627D Not Adding.cpp>)。
