# P5541 题解

排序后，最少移动次数等于 $N$ 减去长度小于 $N$ 的区间中最多能包含的奶牛数，用双指针求这个最大值。

有一种例外：$N-1$ 头已经连续，最后一头离得很远。端点限制使空位不能一步填上，此时最少需要 2 步。

求最多次数时，只需决定最后保留左端还是右端的空隙。答案为

$\max(a_{N-2}-a_0,a_{N-1}-a_1)-N+2$。

排序占 $O(N\log N)$，滑动窗口为 $O(N)$，空间复杂度 $O(N)$。
