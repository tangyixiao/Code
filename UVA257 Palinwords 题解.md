# 题意简析

求出给定的询问中含有至少两个无包含关系且不等的回文子串的字符串。

# 思路解析

考虑到大的回文子串都会被形如：$\tt{ABA}$，$\tt{ABBA}$ 的基础回文子串筛掉，这几个小的回文子串是可以被 $O(n)$ 的时间复杂度扫一遍得出的，大的回文子串就是在两边进行拓展。

然后，我们就需要一个可以快速判断字符串是否相等的技巧，那就是哈希。

我们对字符串关于长度分成不同的桶，因为上文已经提到大的回文子串一定会包含小的回文子串，判断长度相同或相差为 $1$ 的字符串是否相等。