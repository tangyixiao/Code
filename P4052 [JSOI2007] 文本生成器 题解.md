正着统计“至少包含一个单词”的文章不太方便，考虑反过来：

$$
\text{答案}=26^m-\text{一个单词都不包含的文章数}.
$$

于是问题就变成了：有多少个长度为 $m$ 的字符串，不包含给出的任何一个模式串。

显然可以上 AC 自动机。

把所有单词插进 Trie，然后建 fail。对于一个节点，如果它本身是某个单词的结尾，或者它的 fail 指向的状态已经包含某个单词，那么只要走到这个节点，当前字符串就已经出现了至少一个给定单词。

记 `bad[u]` 表示状态 $u$ 是否已经匹配到某个单词，建 fail 时直接做

```cpp
bad[u]|=bad[fail[u]];
```

即可。

然后 DP。

设 `f[i][u]` 表示已经填了 $i$ 个字符，现在 AC 自动机停在状态 $u$，并且到目前为止还没有出现任何给定单词的方案数。

枚举下一个字符 $c$，设

$$
v=tr[u][c].
$$

如果 `bad[v]`，这种方案显然不能要；否则转移

$$
f_{i+1,v}\mathrel{+}=f_{i,u}.
$$

最后把所有状态的 $f_{m,u}$ 加起来，就是完全不可读的文章数量，再用 $26^m$ 减掉即可。

状态数最多是所有模式串长度之和，也就 $6000$ 左右，直接跑就行。

```cpp
#include <bits/stdc++.h>
using namespace std;

namespace TANGYIXIAO {

const int N=6e3+5,MOD=1e4+7;

int n,m,tot;
int tr[N][26],fail[N],bad[N];
int f[2][N];

inline void insert(string s) {
    int u=0;
    for (char c:s) {
        int x=c-'A';
        if (!tr[u][x]) {
            tr[u][x]=++tot;
        }
        u=tr[u][x];
    }
    bad[u]=1;
    return;
}

inline void build() {
    queue<int> q;
    for (int c=0;c<26;c++) {
        if (tr[0][c]) {
            q.push(tr[0][c]);
        }
    }
    while (!q.empty()) {
        int u=q.front();
        q.pop();
        bad[u]|=bad[fail[u]];
        for (int c=0;c<26;c++) {
            if (tr[u][c]) {
                fail[tr[u][c]]=tr[fail[u]][c];
                q.push(tr[u][c]);
            } else {
                tr[u][c]=tr[fail[u]][c];
            }
        }
    }
    return;
}

inline void solve(int Task_Id) {
    cin>>n>>m;
    for (int i=1;i<=n;i++) {
        string s;
        cin>>s;
        insert(s);
    }

    build();

    f[0][0]=1;
    for (int i=0;i<m;i++) {
        int x=i&1,y=x^1;
        memset(f[y],0,sizeof(f[y]));
        for (int u=0;u<=tot;u++) {
            if (!f[x][u]) {
                continue;
            }
            for (int c=0;c<26;c++) {
                int v=tr[u][c];
                if (!bad[v]) {
                    f[y][v]=(f[y][v]+f[x][u])%MOD;
                }
            }
        }
    }

    int no=0;
    for (int u=0;u<=tot;u++) {
        no=(no+f[m&1][u])%MOD;
    }

    int all=1;
    for (int i=1;i<=m;i++) {
        all=all*26%MOD;
    }

    cout<<(all-no+MOD)%MOD<<'\n';

    return;
}

} // namespace TANGYIXIAO

signed main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    TANGYIXIAO::solve(1);

    return 0;
}
```
