# 题意简析

构造数据以卡掉自然溢出哈希。

# 思路解析

## 知识铺垫

要卡掉哈希，我们就必须先知道什么是哈希。

哈希的本质是一种映射，字符串哈希就是将字符串映射到一个哈希值。如果有两个字符串的哈希值不同，那么它们自然就不可能相同。但是，如果两个字符串有相同的哈希值，那么它们也不一定相同，不相同的情况就叫做**哈希冲突**。

由于在赛场上，对于哈希冲突的处理较为麻烦或较为难以实现，所以就像本题中的 VFleaKing 一样，他的哈希就可能会被卡。

### 自然溢出哈希

那什么是自然溢出哈希呢？

在 C++ 中，我们都知道一般来说的变量最大的就是 `unsigned long long` 类型，这个类型可以表示的最大值是 $2^{64}-1$。

我们试想一下，如果一个数超过了数据类型的范围，那么它就会发生溢出。在其他有符号整数类型中，如果一个数超过了它的最大值，那么它就会变成负数，因为其占据了符号位（之所以有的时候溢出值是个正数，可能是因为溢出的太多，导致符号位刚好是 $0$）；在无符号整数类型中，如果一个数超过了它的最大值，那么它就会发生**自然溢出**。

但是溢出的太多会占据其他的变量的地址空间，导致 MLE、RE 等诸多奇奇怪怪的错误。（~~因为上述行为都是 `Undefined Behavior`~~）

这个哈希值的计算的过程是相当于对 $2^{64}$ 取模，哈希值的取值范围就是 $[0, 2^{64})$。

## 卡法

自然溢出哈希的模数非常之大，我们肯定不能用卡大模数哈希的思路来卡自然溢出哈希。这里的模数已知，但是底数未知。所以，我们考虑分析 $2^{64}$ 的性质。

因为题目中的按字符串字典序来处理哈希值，那么我们不妨设底数为 $b$，那么有哈希函数：
$$
\text{Hash}(s) = s_1 \times b^1 + s_2 \times b^2 + \cdots + s_n \times b^{n} \pmod {2^{64}}
$$

也就是：

$$
\text{Hash}(s) = \sum_{i=1}^{n} s_i \times b^{i} \pmod {2^{64}}
$$

