# 题意简析

求 $1$ 到 $2025$ 的排列满足对于 $1 \le i \le j \le 2025$，有 $A_i \times A_j \le i \times j + 2025$ 的个数，答案对 $10^9+7$ 取模。

# 思路解析

题目要求 $A_i \times A_j \le i \times j + 2025$，注意到题目中有这么一个条件：

> $i$ 可能等于 $j$。

那么，对于这一种情况，它要满足的条件是：

$$
A_i^2 \le i^2 + 2025
$$

也就是说:

$$
A_i^2 - i^2 \le 2025 \\
(A_i + i) (A_i - i) \le 2025
$$




想到 $A_i \le i$ 时，是显然对的，因为 $A_i - i$ 非正，所以 $(A_i + i) (A_i - i) \le 0 < 2025$。

考虑 $A_i > i$ 的情况，假如 $i$ 很大，那么一定不满足假设，有没有边界？

因为 $A_i$ 和 $i$ 都是正整数，所以它们肯定满足：

$$
(A_i-1)^2 < A_i^2 < (A_i+1)^2
$$

和

$$
(i-1)^2 < i^2 < (i+1)^2
$$


考虑到要对任意情况满足，我们考虑边界条件 $A_i = i+1$，原式就可以化成：

$$
2i +1 \le 2025 \\
$$

也就是说，$i \le 1012$ 时，有 $A_i > i$（这个东西是充要条件的原因是前面的假设 $A_i = i + 1$），我们也就可以知道 $i=1013$ 时，$A_i \ge 1013$ 和 $i > 1013$ 时，有 $A_i \le i$。

虽说在限定范围下，选择数看起来很多，但是实际上较少，随着 $i$ 的增大，被选择的数也会以相同的斜率增长，所以在 $i \le 1012$ 时，差值不变为 $2$。当 $i=1013$ 时，有 $1013$ 个数，已经被选了 $1012$ 个，所以只能选 $1$ 个，后面的 $i > 1013$，由于小的已经被选尽，那么它们只能选自己了。

那么最终方案数就是 $2^{1012} \times 1^{1013} = 2^{1013}$，答案再对 $10^9+7$ 取模即可。

可以用快速幂实现或直接输出 $781448427$。

# 代码实现

```cpp
#include <bits/stdc++.h>
#define int long long
using namespace std;
namespace Math {
    const int Mod = 1e9 + 7;
    inline int Kuai_Cu_Mi(int Base, int Power) {
        int Ans = 1;
        while (Power) {
            if (Power & 1)
                Ans = (Ans * Base) % Mod;
            Base = (Base * Base) % Mod;
            Power >>= 1;
        }
        return Ans;
    }
}
signed main() {
    // cout << Math::Kuai_Cu_Mi(2, 1012);
    cout << 781448427;
    return 0;
}
```
