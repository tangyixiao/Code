# 算法介绍

直接枚举所有点对的时间复杂度是 $O(n^2)$，显然无法通过。因此可以先求凸包，再在凸包上用**旋转卡壳**。

凸包怎么求？详细请看 [P2742 【模板】二维凸包 / [USACO5.1] 圈奶牛Fencing the Cows](https://www.luogu.com.cn/problem/P2742)。这里介绍一下 Andrew 算法。将所有点按横坐标排序，横坐标相同按纵坐标排序。先从左到右扫描求下凸壳，再从右到左扫描求上凸壳，合并时去重就做完了。实现时注意叉积的符号，如果非正，就说明是顺时针，要弹出。

旋转卡壳到底是什么？考虑枚举凸包上的每一条边 $p_i p_{i+1}$，同时维护一个距离这条边最远的点 $p_j$。当边逆时针转动时，最远点也会一样动。之后分别用 $p_i$ 与 $p_j$、$p_{i+1}$ 与 $p_j$ 来更新答案。

# 正确性证明

我们先确定几个共识：

1. **最远点对一定出现在点集的凸包上**。如果某个端点不在凸包上，将它沿某个方向移到凸包上，距离一定不降，原命题得证。

2. **对踵点对**：存在两条平行线分别经过两点，且凸包位于两线之间，这两个点是一对对踵点。可以证明，凸包的直径就是对踵点对中距离的最大值。 

 
枚举 $p_i p_{i+1}$ 时，另一条平行线恰好切于距离该边最远的点 $p_j$，此时 $(p_i,p_j)$ 和 $(p_{i+1},p_j)$ 均构成对踵点对。叉积 $\overrightarrow{p_{j+1}}$ 比 $\overrightarrow{p_j}$ 更大时，说明 $\overrightarrow{p_{j+1}}$ 比 $\overrightarrow{p_j}$ 离这条边更远，此时将 $j$ 向后移动。

## 复杂度证明  
求凸包为 $O(n \log n)$，旋转卡壳中 $O(m)$，$m$ 为凸包大小。时间复杂度 $O(n \log n)$，瓶颈在于排序。

# 代码实现

```cpp
#include <bits/stdc++.h>
using namespace std;
using ll = long long;
const int N = 5e5 + 5;
int n, m;
struct P {
    int x, y, z;
    bool operator<(const P &rhs) const {
        return x != rhs.x ? x < rhs.x : y < rhs.y;
    }
} a[N], b[N];
ll D(const P &u, const P &v) {
    ll dx = u.x - v.x, dy = u.y - v.y;
    return dx * dx + dy * dy;
}
ll C(const P &o, const P &u, const P &v) {
    return 1LL * (u.x - o.x) * (v.y - o.y) - 1LL * (v.x - o.x) * (u.y - o.y);
}
int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int T;
    cin >> T;
    while (T--) {
        cin >> n;
        for (int i = 1; i <= n; ++i) {
            cin >> a[i].x >> a[i].y;
            a[i].z = i - 1;
        }
        sort(a + 1, a + n + 1);
        m = 0;
        for (int i = 1; i <= n; ++i) {
            for (; m > 1 && C(b[m - 1], b[m], a[i]) <= 0; --m)
                ;
            b[++m] = a[i];
        }
        int k = m;
        for (int i = n - 1; i >= 1; --i) {
            for (; m > k && C(b[m - 1], b[m], a[i]) <= 0; --m)
                ;
            b[++m] = a[i];
        }
        if (m > 1)
            --m;

        if (m == 2) {
            cout << b[1].z << ' ' << b[2].z << '\n';
        } else {
            b[m + 1] = b[1];
            int j = 2, u = b[1].z, v = b[2].z;
            ll mx = D(b[1], b[2]);
            for (int i = 1; i <= m; ++i) {
                for (; C(b[i], b[i + 1], b[j]) < C(b[i], b[i + 1], b[j + 1]);
                     j = (j == m ? 1 : j + 1))
                    ;
                ll d1 = D(b[i], b[j]);
                if (d1 > mx)
                    mx = d1, u = b[i].z, v = b[j].z;
                ll d2 = D(b[i + 1], b[j]);
                if (d2 > mx)
                    mx = d2, u = b[i + 1].z, v = b[j].z;
            }
            cout << u << ' ' << v << '\n';
        }
    }
    return 0;
}
```