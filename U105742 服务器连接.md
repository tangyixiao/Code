# U105742 服务器连接

## 题目描述

有N(1<=N<=2000)个服务器，需要使他们全部连通。
       每个服务器有一个长度为7的字母编码，连接两个服务器的难度为两个服务器字母编码对应位置不同的字母个数。
       衡量一个连通方案的质量为1/∑d(to,td),d(to,td)为to和td这2个服务器连接难度。

## 输入格式

数据有多组，输入遇到“0”即表示文件结束。
       对于每组数组，第一行输入一个整数N，表示服务器个数。
       接下来N行，每行一个字符串表示服务器编码。

## 输出格式

输出只有一行，连通方案的质量。输出按照以下格式:
       “1/Q”(不包括引号);
       Q表示总连通方案难度，即∑d(to,td)。

## 输入输出样例 #1

### 输入 #1

```
4
aaaaaaa
baaaaaa
abaaaaa
aabaaaa
0
```

### 输出 #1

```
1/3
```

## 说明/提示

连接服务器1-2、1-3、1-4就能使所有服务器连通。