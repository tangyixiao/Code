# P5518 [MtOI2019] 幽灵乐团 _ 莫比乌斯反演基础练习题 题解

对每个质数 $q$ 单独统计原式的指数。记 $v_q(x)$ 为 $q$ 的次数，则

$$v_q\left(\frac{\operatorname{lcm}(i,j)}{\gcd(i,k)}\right)=v_q(i)+v_q(j)-\min(v_q(i),v_q(j))-\min(v_q(i),v_q(k)).$$

利用 $\sum_{i\le n}v_q(i)=\sum_{t\ge1}\lfloor n/q^t\rfloor$，可以分别求出权为 $1$ 和 $ijk$ 时的指数。第二种权值中，$q^t\mid i$ 的所有 $i$ 之和为 $q^tS(\lfloor n/q^t\rfloor)$，其中 $S(n)=n(n+1)/2$。

第三种权值使用

$$\gcd(i,j,k)=\sum_{d\mid i,j,k}\varphi(d).$$

令 $i=di',j=dj',k=dk'$，公共的 $v_q(d)$ 在上式四项中恰好抵消，因此只需枚举 $d$，把 $\varphi(d)$ 乘到缩小后的第一种指数上。全部指数对 $p-1$ 取模，最后计算各质数幂的乘积。

每次询问的枚举量约为 $\sum_d\pi(\max(A,B)/d)$，复杂度可写作 $O(n\log\log n)$，空间复杂度 $O(n)$。
