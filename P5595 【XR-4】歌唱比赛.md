# P5595 【XR-4】歌唱比赛

## 中文题面

主持人从个位开始，逐步比较两个非负整数当前已经看到的后缀。小 X 较大记为 `X`，小 Y 较大记为 `Y`，相等记为 `Z`。给定比较结果串 $s$，构造一对等长、允许前导零的十进制数；无解输出 `-1`。

### 输入

一个只含 `X`、`Y`、`Z` 的字符串 $s$，$1\le |s|\le10^6$。

### 输出

有解时输出两个长度均为 $|s|$ 的数，否则输出 `-1`。本题为 Special Judge。

### 样例

```text
XY
```

一种合法输出为：

```text
37
28
```

其余公开样例为：

```text
XYZ
```

```text
137
047
```

```text
ZZZZZZ
```

```text
000000
000000
```

```text
XYZXYZ
```

```text
-1
```

题目共有四档：$|s|=1$；只含 X/Y；保证有解；无特殊限制。

## English Statement

Compare two numbers from the least significant digit toward the most significant digit. After each newly exposed digit, record `X`, `Y`, or `Z` according to the comparison of the visible suffixes. Construct two equal-length decimal strings, allowing leading zeroes, or print `-1` if impossible. The length is at most $10^6$.
