# P3803 【模板】多项式乘法（FFT）

## 中文题面

### 题目描述

给定一个 $n$ 次多项式 $F(x)$，和一个 $m$ 次多项式 $G(x)$。

请求出 $F(x)$ 和 $G(x)$ 的乘积。

### 输入格式

第一行两个整数 $n,m$。

接下来一行 $n+1$ 个整数，从低到高表示 $F(x)$ 的系数。

接下来一行 $m+1$ 个整数，从低到高表示 $G(x)$ 的系数。

### 输出格式

一行 $n+m+1$ 个整数，从低到高表示 $F(x) \cdot G(x)$ 的系数。

### 样例 1

```text
1 2
1 2
1 2 1
```

```text
1 4 5 2
```

### 说明与限制

保证输入中的系数均为大于等于 $0$ 且小于等于 $9$ 的整数。

对于 $100\%$ 的数据：$0 \le n, m \leq {10}^6$。

## English Statement

### Description



### Input



### Output



### Constraints
