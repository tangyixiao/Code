请帮我用C++解决20260520pro.pdf中的问题，除建图建树外不要用vector，静态数组定义在主函数外，相同类型成一行排列，如果在定义时有一样的数值，请用常量表示，不要在函数里传vector、数组等，不要加注释，数组名、变量名尽可能地短，输入输出用cin、cout，加上文件读写，文件读写格式如下：
```cpp
// 假设为d题
freopen("d.in", "r", stdin);
freopen("d.out", "w", stdout);
```