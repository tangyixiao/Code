# U200539 棋盘旅行

## 题目描述

有一个 $8\times 8$ 的棋盘，每个格子上有个小写字母，记为 $B(x,y)$。

给出一个长度为 $k$ 的目标串，记为 $s_1,s_2,\cdots,s_k$。询问有多少条国王的旅行

路线 $(x_1, y_1),\cdots,(x_k, y_k)$满足以下要求：

- $1\leq x_i,y_i\leq 8$，$\forall 1\leq i\leq k$。
- $B(x_i,y_i)=s_i$，$\forall 1\leq i\leq k$。
- $(x_i,y_i)\not=(x_j,y_j)$，$\forall 1\leq i<j\leq k$。
- $\max(|x_i − x_{i+1}|, |y_i − y_{i+1}|) = 1$，$\forall 1\leq i\leq k$。

## 输入格式

第一行，一个整数，表示 $k$。

第二行，一个长度为 $k$的字符串，描述目标串。

接下来 $8$ 行，每行 $8$ 个字符，描述棋盘。

目标串和棋盘仅有小写字母构成。

## 输出格式

一共一行，包含一个整数，表示满足要求的旅行路线的数量。

## 输入输出样例 #1

### 输入 #1

```
2
aa
aabbbbbb
aabbbbbb
bbbbbbbb
bbbbbbbb
bbbbbbbb
bbbbbbbb
bbbbbbbb
bbbbbbbb
```

### 输出 #1

```
12
```

## 输入输出样例 #2

### 输入 #2

```
9
checkmate
checkmat
checkmee
checkmat
checkmee
checkmat
checkmee
checkmat
checkmee
```

### 输出 #2

```
7698
```

## 说明/提示

$40\%$：$1\leq k\leq 8$。

$100\%$：$1\leq k\leq 11$。