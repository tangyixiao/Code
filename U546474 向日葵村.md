# U546474 向日葵村

## 题目背景

传说很久很久以前，奶龙大陆上有一座雪山。在雪山旁边，有一个民风淳朴的村庄。由于村庄旁长了很多向日葵，这个村就叫做向日葵村。

向日葵村的村民们觉得这个雪山太高了，太牛了，简直是神山啊。刚好，村民们崇拜大坝，他们就把这座雪山命名为大坝山。

有一只名叫尔多龙的恶龙住在大坝山的山顶。尔多龙经常下山骚扰向日葵村，村民们不堪其扰，于是展开了一场勇者选拔赛，挑出全村最牛的勇者上山与尔多龙决斗。

## 题目描述

向日葵村的 $n$ 个勇者站成一排，第 $i$ 个勇者的能力值为 $a_i$。

共有 $q$ 次操作，包括以下两种：
- `1 l r x`：对于所有 $i\in[l,r]$，将 $a_i$ 加上 $x$。
- `2 l r`：查询区间 $a_l\le a_{l+1}\le\cdots\le a_r$ 是否成立。

## 输入格式

第一行两个整数 $n,q$ 代表勇者个数与操作数。  

第二行 $n$ 个整数 $a_i$ 代表每个勇者的能力值。   

接下来 $q$ 行，每行首先三个整数 $opt,l,r$：

- 如果 $opt=1$，接下来一个整数 $x$ 代表操作 $1$。
- 如果 $opt=2$，代表操作 $2$。

由于数据故障，$r$ 可能取到 $n+1$，请在这类情况下自行令 $r=n$，谢谢。

## 输出格式

对于每个操作 $2$，输出询问结果。

## 输入输出样例 #1

### 输入 #1

```
7 5
1 9 1 9 8 1 0
2 1 3
1 3 4 9
2 1 4
1 5 6 11
2 2 6
```

### 输出 #1

```
No
Yes
No
```

## 说明/提示

#### 【数据范围】

**本题采用捆绑测试。**

- Subtask 1（1 pts）：询问操作的区间长度均为 $1$。
- Subtask 2（9 pts）：$n,q \le 10^3$。
- Subtask 3（10 pts）：$n,q\le 5\times 10^3$。
- Subtask 4（10 pts）：只有查询操作。
- Subtask 5（10 pts）：修改操作的数量不超过 $100$。
- Subtask 6（20 pts）：$n,q \le 10^5$。
- Subtask 7（40 pts）：无特殊限制。

对于 $100\%$ 的数据，$1 \le n,q \le 10^6$，$|a_i|,|x| \le 10^9$。

#### 【提示】

本题输入量较大，建议使用快读：
```cpp
namespace FastIO {
    char buf[1 << 23], *p1 = buf, *p2 = buf;
#define getchar() \
    (p1 == p2 && (p2 = (p1 = buf) + fread(buf, 1, 1 << 21, stdin), p1 == p2) ? EOF : *p1 ++)
    inline int read() {
        register int sr = 0;
        register char ch = getchar(), last = 0;
        while (ch < '0' || ch > '9') last = ch, ch = getchar();
        while (ch >= '0' && ch <= '9') sr = (sr << 1) + (sr << 3) + (ch ^ 48), ch = getchar();
        return last == '-' ? -sr : sr;
    }
}
```