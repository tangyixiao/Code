# P13825 题解

初始值不必建树。区间 $[l,r]$ 的初始和可直接用

$\frac{(l+r)(r-l+1)}2$

计算，线段树只维护所有区间加操作带来的增量。

$n$ 达到 $10^9$，不能开普通的 $4n$ 线段树。动态开点，只在修改经过的节点建立左右儿子。节点保存增量区间和与懒标记；查询时把祖先懒标记累加到不存在的子树上。

每次操作时间复杂度 $O(\log n)$，最多建立 $O(m\log n)$ 个节点。30% 部分分直接修改数组，50% 部分分使用普通线段树。
