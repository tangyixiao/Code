我们考虑将每个小鱼看成一个点，然后将每两个距离满足 $\le R$ 的点连上一条边。

我们如果直接考虑去 DFS，暴力去选择每一个点，那么时间复杂度将是 $\mathcal{O}(2^n)$，在 $n \le 50$ 的数据下显然无法通过此题。

我们考虑到能和之前被选过的点有连边的点一定不会被再选到，因为这里是“捕鱼”，鱼捕走了就没有了。所以这就等同于一个求图上最小独立支配集问题，我们都知道这个是一个 NP-Hard 的问题，然后考虑如何剪枝和优化常数。

我们想到 $n^2 \le 2500$，如果每次去搜一下肯定是在常数会大，所以考虑预处理这些点。把这些点按照联通块分割，考虑在随机数据下，显然两个联通块的复杂度就可以直接在指数上砍半，更不要说更多联通块了。我们再按照贪心把点根据能够连的边的数量进行排序，这样做的原因是进行一个最优化剪枝，尽早确定上界。

注意到 $n \le 50$，那么 $2^{50} < 2^{63}-1$，也就是说我们可以进行状态压缩，用一个 `long long` 类型就可以轻松存下。

再在前面的基础上，按照编号从小到大遍历，理清思路，而这个可以用 `__builtin_ctzll()` 优化。然后用 `x &= x - 1` 快速把 $1$ 变成 $0$，你就能轻松愉快的通过此题了！