# P13825 【模板】线段树 1.5

## 中文题面

长度为 $n$ 的数列初始满足 $a_i=i$。支持两种操作：

- `1 l r k`：区间 $[l,r]$ 全部加 $k$；
- `2 l r`：查询区间和。

### 输入

第一行 $n,m$，随后 $m$ 次操作。

### 输出

对每次查询输出一行答案。

### 样例

```text
5 5
2 2 4
1 2 3 2
2 3 4
1 1 5 1
2 1 4
```

```text
9
9
18
```

### 数据范围

30%：$n\le8,m\le10$；50%：$n\le10^5$；100%：$n\le10^9$，$m,k\le10^5$。

## English Statement

Initially $a_i=i$ for a sequence of length $n$. Support range addition and range-sum queries. There are at most $10^5$ operations, while $n$ may reach $10^9$.
