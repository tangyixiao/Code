# P4139 上帝与集合的正确用法 题解

设稳定值为 $F(p)$。指数塔足够高，因此即使底数与模数不互质，也可在指数对 $\varphi(p)$ 取模后补上一个 $\varphi(p)$：

$$F(p)=2^{F(\varphi(p))+\varphi(p)}\bmod p,\qquad F(1)=0.$$

先线性筛出 $10^7$ 以内的欧拉函数，每次询问沿欧拉函数链递归，链长很短。
