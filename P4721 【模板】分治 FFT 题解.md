# P4721 【模板】分治 FFT 题解

递推式中，每个已知的 $f_i$ 会给所有更靠后的项贡献 $f_i g_j$。对下标区间做分治：先递归求完左半区，再用一次 NTT 卷积把左半区的所有贡献批量加入右半区，最后递归处理右半区。这样在处理一个区间时，左侧系数已经完全确定。

每层卷积总规模为 $O(n)$，共有 $O(\log n)$ 层，复杂度 $O(n\log^2 n)$，空间复杂度 $O(n)$。
