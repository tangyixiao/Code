# 题意简析

# 思路解析

一个显然错误的想法是我们观察到样例中 $9$ 和 $1$ 都是完全平方数，因此我们可以想到直接判断 $n$ 是否是完全平方数，然后输出 $Y$ 或者 $N$。但是这样显然是错的，为什么？给出反例：

例如 $12 = 2^2 \times 3$，因此 $12$ 不是完全平方数。但是其因数有：$1, 2, 3, 4, 6, 12$。先手第一步肯定选择 $12$，然后后手为了让先手的 $\gcd$ 为 $1$，那么它一定会选 $6$，接下来无论先手选择 $2,3,4$，那么后手就一定可以选择 $4,2$ 来使先手必输。所以我们只考虑平方数的方法是错误的。

~~虽然这是 ICPC 的题目，但是这样做能够过 41 个点。~~

---



# 代码实现

## 错误实现

```cpp
#include <bits/stdc++.h>
#define int long long
using namespace std;
int n;
signed main() {
    cin >> n;
    if ((int)(sqrt(n)) * ((int)(sqrt(n))) == n) {
        cout << "N\n";
    } else {
        cout << "Y\n";
    }
    return 0;
}
```

## 正确实现

```cpp

```