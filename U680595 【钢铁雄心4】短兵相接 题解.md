**题解：钢铁雄心4 · 兰彻斯特会战模拟**

---

### 题意回顾

给定红军 \(N\) 个师、蓝军 \(M\) 个师，每个师有组织度 \(hp\) 和攻击力 \(atk\)。在连续时间中，所有存活师同时开火，伤害均匀分配：

- 每个红军师每秒受到伤害 \(v_A = \dfrac{\sum_{蓝军存活} atk}{\text{红军存活数}}\)  
- 每个蓝军师每秒受到伤害 \(v_B = \dfrac{\sum_{红军存活} atk}{\text{蓝军存活数}}\)

师的组织度降为 \(0\) 时瞬间溃退。求最后哪方获胜，并输出获胜方剩余总组织度（存活师剩余 \(hp\) 之和）。

---

### 核心思路——离散事件模拟

战斗过程中，双方的总攻击力和受损速率只有在有师阵亡时才会改变。因此，我们可以将“有师死亡”作为事件，逐步推进时间线，而不是按固定时间步长模拟（那样会超时）。每次事件后，至少有一个师消失，总事件数不会超过 \(N+M\)。

**如何找到下一个阵亡的师？**  
设当前红军累积伤害为 \(D_A\)，蓝军累积伤害为 \(D_B\)。对于红军师 \(i\)，其剩余组织度为 \(hp_i - D_A\)，它会在 \(t = \dfrac{hp_i - D_A}{v_A}\) 秒后阵亡。同理，蓝军师 \(j\) 会在 \(t = \dfrac{hp_j - D_B}{v_B}\) 秒后阵亡。显然，下一事件的发生时间就是所有存活师中“阵亡所需时间”的最小值。为了快速得到这个最小值，我们只需比较双方当前组织度最小的师（因为分子最小，分母相同），即：

- 红军最早阵亡时间：\(dt_A = \dfrac{\min_{i\in A}\{hp_i\} - D_A}{v_A}\)  
- 蓝军最早阵亡时间：\(dt_B = \dfrac{\min_{j\in B}\{hp_j\} - D_B}{v_B}\)

取 \(\Delta t = \min(dt_A, dt_B)\)，然后更新累积伤害：
\[
D_A \mathrel{+}= v_A \cdot \Delta t,\quad D_B \mathrel{+}= v_B \cdot \Delta t
\]
接着移除那些 \(hp_i \le D_A\) 或 \(hp_j \le D_B\) 的师（可能不止一个，特别是经过多轮累积后低血量师会同时死亡），并更新存活数及总攻击力。

---

### 数据结构与实现

为了能够 \(O(1)\) 获得当前存活的最小 \(hp\)，我们将双方的师按组织度升序排序，然后用指针 `ia`、`ib` 分别指向当前存活的最左侧（即最小 \(hp\) 的师）。每次移除师时，只需右移指针并更新攻击力总和。

具体步骤：

1. 读入数据，分别存储两个 `vector<pair<ll,ll>>`，同时计算总组织度 \(sum\_hp\) 和总攻击力 \(sum\_atk\)（用于最后计算剩余总组织度）。
2. 分别按 \(hp\) 升序排序。
3. 初始化存活数 `na = N, nb = M`，累积伤害 `DA = 0.0, DB = 0.0`，指针 `ia = ib = 0`。
4. 当双方存活数均大于 0：
   - 计算受损速率 `vA = sumB_atk / na`，`vB = sumA_atk / nb`。
   - 获得当前最小剩余 \(hp\)：`curA = A[ia].hp - DA`，`curB = B[ib].hp - DB`。
   - 计算死亡时间 `dtA = curA / vA`，`dtB = curB / vB`（若 `vA` 可能为 0？根据题意 atk>=1 且存活数>0 时不会为 0）。
   - 若 `dtA <= dtB + eps`，则事件为红军先阵亡（或同时）：
     * 经过时间 `dt = dtA`。
     * 更新 `DA += vA * dt`，`DB += vB * dt`。
     * 循环：只要 `ia < N` 且 `A[ia].hp <= DA + eps`，将该师从总攻击力中扣除，`na--`，`ia++`。
   - 否则蓝军先阵亡，对称处理。
5. 一方全灭后，可能胜方还有师在事件中刚好达到阵亡条件，需再次清理（即循环移除 `hp <= D_x` 的师）。
6. 胜方剩余总组织度 = 该方初始总 \(hp\) - （存活数 × 该方累积伤害）。注意浮点误差，若为负取 \(0\)。

**浮点误差处理**：所有比较使用 `eps = 1e-12`，例如 `A[ia].hp <= DA + eps` 判定死亡。

**时间复杂度**：排序 \(O(N\log N + M\log M)\)，模拟过程每名阵亡师只被指针扫描一次，共 \(O(N+M)\)。总复杂度 \(O((N+M)\log(N+M))\)，在 \(2\times 10^5\) 范围内能轻松运行。

---

### 注意事项

- 由于 \(hp\) 和 \(atk\) 最高达 \(10^9\)，累积伤害和剩余组织度的计算仍在 `double` 精度内（53 位尾数有效数字约 15~16 位十进制），足以支持 \(10^9\) 相加不丢失整数精度，但速率除法可能产生长尾小数。使用 `eps` 判定死亡比较非常重要。
- 计算剩余总组织度时，不要直接累加每个师的剩余 \(hp\)（可能因多次浮点加减累积误差），更好的方法是使用：`总初始hp总和 - 存活数 * 当前累积伤害`，这样误差主要来源于最后一步的乘法和减法，可接受。

---

### 参考代码（C++）

```cpp
#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using pll = pair<ll, ll>;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int N, M;
    cin >> N >> M;
    vector<pll> A(N), B(M);
    ll sumA_hp = 0, sumA_atk = 0;
    for (auto &[hp, atk] : A) {
        cin >> hp >> atk;
        sumA_hp += hp;
        sumA_atk += atk;
    }
    ll sumB_hp = 0, sumB_atk = 0;
    for (auto &[hp, atk] : B) {
        cin >> hp >> atk;
        sumB_hp += hp;
        sumB_atk += atk;
    }

    sort(A.begin(), A.end());
    sort(B.begin(), B.end());

    int ia = 0, ib = 0, na = N, nb = M;
    double DA = 0.0, DB = 0.0;
    const double eps = 1e-12;

    while (na > 0 && nb > 0) {
        double vA = (double)sumB_atk / na;
        double vB = (double)sumA_atk / nb;

        double curA = A[ia].first - DA;
        double curB = B[ib].first - DB;
        double dtA = curA / vA;
        double dtB = curB / vB;

        if (dtA <= dtB + eps) {
            double dt = dtA;
            DA += vA * dt;
            DB += vB * dt;
            while (ia < N && A[ia].first <= DA + eps) {
                sumA_atk -= A[ia].second;
                sumA_hp -= A[ia].first;   // 可选，用于最后计算剩余
                --na;
                ++ia;
            }
        } else {
            double dt = dtB;
            DB += vB * dt;
            DA += vA * dt;
            while (ib < M && B[ib].first <= DB + eps) {
                sumB_atk -= B[ib].second;
                sumB_hp -= B[ib].first;
                --nb;
                ++ib;
            }
        }
    }

    // 清理胜方可能同时死亡的师
    if (na > 0) {
        while (ia < N && A[ia].first <= DA + eps) {
            sumA_hp -= A[ia].first;
            --na;
            ++ia;
        }
        double ans = sumA_hp - na * DA;
        if (ans < 0) ans = 0.0;
        cout << "A " << fixed << setprecision(6) << ans << '\n';
    } else {
        while (ib < M && B[ib].first <= DB + eps) {
            sumB_hp -= B[ib].first;
            --nb;
            ++ib;
        }
        double ans = sumB_hp - nb * DB;
        if (ans < 0) ans = 0.0;
        cout << "B " << fixed << setprecision(6) << ans << '\n';
    }

    return 0;
}
```

---

### 总结

本题利用了连续战斗中仅当有单位阵亡时才需要更新状态的特性，转化为离散事件模拟。核心是快速找到下一个阵亡单位——通过维护排序后的最小 \(hp\)，每次 \(O(1)\) 比较即可确定。总模拟步数线性，配合高效的排序，足以应对最大数据范围。