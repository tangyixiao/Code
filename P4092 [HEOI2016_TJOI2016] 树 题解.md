比较裸的树剖。

要求的是 $u$ 到根这条链上深度最大的已标记点。考虑树链剖分以后维护每个点是否被标记，在线段树的 $dfn_u$ 位置存 $dfn_u$，没被标记就存 $0$。

为什么可以直接取最大的 `dfn`？因为对于祖先关系，一定有

$$
u\text{ 是 }v\text{ 的祖先}\Rightarrow dfn_u<dfn_v.
$$

而一次询问中考虑的所有点都在根到 $u$ 的同一条路径上，所以其中 `dfn` 最大的已标记点就是深度最大的那个，也就是最近的已标记祖先。

于是询问时正常树剖跳链，把 $1\to u$ 拆成 $O(\log n)$ 个连续区间，每个区间在线段树上查询最大值即可。

标记操作就是单点修改。重复标记没有影响，重新赋成 `dfn[u]` 即可。

最后用 `rk[dfn]` 找回节点编号。

复杂度就是单次修改 $O(\log n)$，单次询问 $O(\log^2 n)$。

```cpp
#include <bits/stdc++.h>
using namespace std;

namespace TANGYIXIAO {

const int N=1e5+5;

int n,q;
vector<int> g[N];

int fa[N],dep[N],siz[N],son[N];
int top[N],dfn[N],rk[N],tim;
int tr[N<<2];

inline void dfs1(int u,int f) {
    fa[u]=f;
    dep[u]=dep[f]+1;
    siz[u]=1;
    for (int v:g[u]) {
        if (v==f) {
            continue;
        }
        dfs1(v,u);
        siz[u]+=siz[v];
        if (siz[v]>siz[son[u]]) {
            son[u]=v;
        }
    }
    return;
}

inline void dfs2(int u,int t) {
    top[u]=t;
    dfn[u]=++tim;
    rk[tim]=u;
    if (son[u]) {
        dfs2(son[u],t);
    }
    for (int v:g[u]) {
        if (v==fa[u]||v==son[u]) {
            continue;
        }
        dfs2(v,v);
    }
    return;
}

inline void change(int p,int l,int r,int x) {
    if (l==r) {
        tr[p]=l;
        return;
    }
    int mid=(l+r)>>1;
    if (x<=mid) {
        change(p<<1,l,mid,x);
    } else {
        change(p<<1|1,mid+1,r,x);
    }
    tr[p]=max(tr[p<<1],tr[p<<1|1]);
    return;
}

inline int query(int p,int l,int r,int x,int y) {
    if (x<=l&&r<=y) {
        return tr[p];
    }
    int mid=(l+r)>>1,res=0;
    if (x<=mid) {
        res=max(res,query(p<<1,l,mid,x,y));
    }
    if (y>mid) {
        res=max(res,query(p<<1|1,mid+1,r,x,y));
    }
    return res;
}

inline int ask(int u) {
    int res=0;
    while (top[u]!=1) {
        res=max(res,query(1,1,n,dfn[top[u]],dfn[u]));
        u=fa[top[u]];
    }
    res=max(res,query(1,1,n,dfn[1],dfn[u]));
    return rk[res];
}

inline void solve(int Task_Id) {
    cin>>n>>q;
    for (int i=1;i<n;i++) {
        int u,v;
        cin>>u>>v;
        g[u].push_back(v);
        g[v].push_back(u);
    }

    dfs1(1,0);
    dfs2(1,1);

    change(1,1,n,dfn[1]);

    for (int i=1;i<=q;i++) {
        char op;
        int x;
        cin>>op>>x;
        if (op=='C') {
            change(1,1,n,dfn[x]);
        } else {
            cout<<ask(x)<<'\n';
        }
    }

    return;
}

} // namespace TANGYIXIAO

signed main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    TANGYIXIAO::solve(1);

    return 0;
}
```
